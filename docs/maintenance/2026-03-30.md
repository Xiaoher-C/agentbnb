# AgentBnB Daily Maintenance Report — 2026-03-30

## Executive Summary
Build and typecheck are green on `main`, but today's full Vitest run is not yet a reliable regression signal because it ran under local Node `v25.6.1` plus sandboxed socket restrictions, producing `681/1496` passing with failures dominated by a `better-sqlite3` native ABI mismatch and `EPERM` bind/listen errors. Dependency audit also surfaced a critical `handlebars` issue in `packages/genesis-template` and a high-severity `path-to-regexp` issue via `@modelcontextprotocol/sdk`, so security remediation and a clean runtime rerun are today's priorities.

## 🔴 Critical (Action Required Today)
- Full test suite is red in the current workstation environment: `681 passed / 806 failed / 9 skipped / 1496 total` vs baseline `1461` tests. Dominant causes are `better-sqlite3` native ABI mismatch (`NODE_MODULE_VERSION 137` vs runtime `141` under local Node `v25.6.1`) and sandbox `EPERM` errors when tests bind `127.0.0.1` or `0.0.0.0:5353`. Rebuild native deps under a supported Node version and rerun outside socket restrictions before treating this as product regression.
- `pnpm audit` reports `18` vulnerabilities overall (`1 critical`, `7 high`). Highest-risk confirmed items:
  - `handlebars@4.7.8` in `packages/genesis-template` with multiple GHSA advisories, including one critical; patch target is `>=4.7.9`.
  - `path-to-regexp@8.3.0` via `@modelcontextprotocol/sdk > express > router`; patch path requires lifting to `>=8.4.0` through dependency updates.
- Identity convergence is still incomplete: canonical `agent_id` exists, but runtime/config/credit/relay paths still lean on `owner`, leaving alias drift risk across routing and ledger ownership.

## 🟡 Warning (Address This Week)
- Docs are drifting from repo reality: `AGENT-NATIVE-PROTOCOL.md`, `docs/adr/`, `docs/api/`, and `docs/architecture/` links are stale or missing; `IDENTITY-MODEL.md` lives at repo root, not under `docs/`.
- Outdated packages with likely breaking upgrade work:
  - `better-sqlite3 11.10.0 -> 12.8.0`
  - `zod 3.25.76 -> 4.3.6`
  - `commander 12.1.0 -> 14.0.3`
  - `vitest 2.1.9 -> 4.1.2`
  - `typescript 5.9.3 -> 6.0.2`
  - `eslint 9.39.4 -> 10.1.0`
- Smaller safe lifts are available for `fastify 5.8.2 -> 5.8.4`, `@modelcontextprotocol/sdk 1.27.1 -> 1.28.0`, and `ws 8.19.0 -> 8.20.0`. `@anthropic-ai/sdk` appears current; `tweetnacl` is not in the dependency tree.
- Git/release hygiene is slightly behind: `package.json` and npm are `8.4.2`, but the latest git tag is still `v8.3.0`.
- Packaging is mostly clean, but `pnpm pack --dry-run` shows `skills/agentbnb/*.ts` and `.test.ts` files entering the published tarball. This is not a dependency leak, but it is release noise worth tightening.
- One remote branch (`core/feat/core-node-foundation`) is both merged-safe and stale (`>7` days old).

## 🟢 Healthy
- `git pull --ff-only origin main` reports the local checkout is already up to date on `main`.
- `pnpm build` and `pnpm tsc --noEmit` both pass locally.
- Test inventory has grown, not shrunk: current suite size is `1496` vs baseline `1461` (`+35`).
- Lockfile integrity is good: `pnpm install --frozen-lockfile` succeeds.
- Codebase tech debt remains low-volume: `2` TODOs, `0` FIXME/HACK/XXX, `16` `as any` casts, and `0` `@ts-ignore` / `@ts-expect-error`.
- No tracked secrets were found; `.gitignore` covers `node_modules/`, `dist/`, `.env*`, SQLite files, logs, coverage, and private docs.
- CI is wired on PRs to `main` and runs install, lint, backend tests, and hub tests.
- `agentbnb status` fallback for new agents appears fixed, and OpenClaw tool exposure is implemented.

## Metrics
- Tests: `681/1496` passing locally (baseline `1461`; `+35` tests, but environment-invalid due Node/sandbox issues)
- Build: ✅
- Security: `18` vulnerabilities total (`1 critical`, `7 high`); production audit still shows `13` total (`1 critical`, `5 high`)
- Tech Debt: `2` TODOs, `16` type-safety violations (`as any`)
- Branches: `1` merged-safe and stale branch to clean up report-only

## Recommended Actions (Priority Order)
1. Rerun the full suite under a supported Node toolchain (prefer Node 20 or 24), rebuild `better-sqlite3`, and run outside sandbox socket restrictions so today's failures can be separated from real regressions.
2. Patch `packages/genesis-template` to `handlebars >= 4.7.9`, then update `@modelcontextprotocol/sdk` and its `path-to-regexp` chain, and sweep the remaining dev-tool advisories (`flatted`, `picomatch`).
3. Finish the `owner` -> `agent_id` convergence and clean the stale doc paths/tagging gap (`v8.4.2` package published but no matching git tag, missing protocol/ADR docs, stale remote branch).

## Changes Since Last Report
- No prior `docs/maintenance/` report was present in the repo; using recent mainline activity as the baseline.
- `d1df9e2` bumped npm package version to `8.4.2`.
- `64ef472` shipped the OpenClaw integration bundle (`v8.4.1`).
- `a35cb3e` added Telegram failure notifications and the OpenClaw setup command (`v8.4`).
- `3f72161` fixed nvm CLI resolution and added graceful status handling for new agents.
