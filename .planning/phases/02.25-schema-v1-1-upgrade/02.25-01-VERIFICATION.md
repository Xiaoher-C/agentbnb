---
phase: 02.25-schema-v1-1-upgrade
verified: 2026-03-15T03:15:00Z
status: gaps_found
score: 9/10 must-haves verified
re_verification: false
gaps:
  - truth: "CLI discover --json output never contains _internal field"
    status: partial
    reason: "Code is present and correct (line 321 of src/cli/index.ts strips _internal before both JSON and table output), but the CLI integration test suite does not include a dedicated test asserting _internal is absent from --json output. The stripping is structurally correct but the contractual test coverage for this truth is missing."
    artifacts:
      - path: "src/cli/index.ts"
        issue: "Stripping code exists and is correct — no test in src/cli/index.test.ts verifies _internal is absent from --json discover output"
    missing:
      - "Add CLI integration test: publish card with _internal, run discover --json, assert _internal key is absent from output"
  - truth: "Requirements SCH-02 through SCH-06 are traceable to REQUIREMENTS.md"
    status: failed
    reason: "The PLAN frontmatter declares requirements: [SCH-02, SCH-03, SCH-04, SCH-05, SCH-06] but none of these IDs appear anywhere in .planning/REQUIREMENTS.md. The canonical requirements file has no SCH-series requirements section. The PLAN invented requirement IDs that were never registered in REQUIREMENTS.md."
    artifacts:
      - path: ".planning/REQUIREMENTS.md"
        issue: "No SCH-02, SCH-03, SCH-04, SCH-05, or SCH-06 entries exist"
      - path: ".planning/phases/02.25-schema-v1-1-upgrade/02.25-01-PLAN.md"
        issue: "Declares requirements: [SCH-02, SCH-03, SCH-04, SCH-05, SCH-06] which are not in REQUIREMENTS.md"
    missing:
      - "Add SCH-02 through SCH-06 to REQUIREMENTS.md, OR update PLAN frontmatter to reference the correct existing requirement IDs that cover _internal field, free_tier field, server stripping, CLI stripping, and Hub badge"
---

# Phase 02.25: Schema v1.1 Upgrade Verification Report

**Phase Goal:** Add _internal and free_tier fields to CapabilityCardSchema, strip _internal from API/CLI responses, render free-tier badges in Hub. Additive optional fields, backward compatible with v1.0. powered_by already implemented (commit 7946aab).
**Verified:** 2026-03-15T03:15:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                              | Status      | Evidence                                                                                                        |
|----|-------------------------------------------------------------------|-------------|----------------------------------------------------------------------------------------------------------------|
| 1  | Cards with _internal field validate successfully                  | VERIFIED    | `src/types/index.ts` line 55: `_internal: z.record(z.unknown()).optional()`. Test at index.test.ts line 132.   |
| 2  | Cards without _internal field still validate (backward compat)    | VERIFIED    | Optional field — existing validCard fixture has no _internal. Test at index.test.ts line 141.                  |
| 3  | Cards with free_tier in pricing validate successfully             | VERIFIED    | `src/types/index.ts` line 44: `free_tier: z.number().nonnegative().optional()`. Test at index.test.ts line 154.|
| 4  | Cards without free_tier still validate (backward compat)          | VERIFIED    | Optional field. Test at index.test.ts line 149.                                                                |
| 5  | Negative free_tier is rejected by schema                          | VERIFIED    | nonnegative() constraint. Test at index.test.ts line 175: `free_tier: -1` → `success: false`.                 |
| 6  | GET /cards response never contains _internal field                | VERIFIED    | `server.ts` line 178: `.map(stripInternal)`. Test at server.test.ts line 380: asserts `_internal` undefined.  |
| 7  | GET /cards/:id response never contains _internal field            | VERIFIED    | `server.ts` line 197: `reply.send(stripInternal(card))`. Test at server.test.ts line 401.                     |
| 8  | CLI discover --json output never contains _internal field         | PARTIAL     | Strip code at cli/index.ts line 321 is correct. No CLI integration test asserts absence of _internal in output.|
| 9  | Hub renders free-tier badge when free_tier > 0                    | VERIFIED    | CapabilityCard.tsx lines 100-105. Hub test passes: "50 free/mo" found in render.                              |
| 10 | Hub does not render free-tier badge when free_tier absent or 0    | VERIFIED    | Guard uses `!== undefined && > 0`. Hub test passes: queryByText(/free\/mo/) is null for absent free_tier.     |

**Score:** 9/10 truths verified (Truth 8 is partial — code correct, test coverage absent)

### Required Artifacts

| Artifact                                    | Expected                                      | Status    | Details                                                                             |
|---------------------------------------------|-----------------------------------------------|-----------|--------------------------------------------------------------------------------------|
| `src/types/index.ts`                        | _internal and free_tier Zod fields            | VERIFIED  | _internal at line 55, free_tier at line 44 within pricing object. JSDoc present.    |
| `src/registry/server.ts`                    | stripInternal helper applied to both endpoints| VERIFIED  | stripInternal defined at line 39, applied at line 178 (GET /cards) and 197 (/:id).  |
| `hub/src/types.ts`                          | HubCard.pricing.free_tier type                | VERIFIED  | Line 21: `pricing: { credits_per_call: number; credits_per_minute?: number; free_tier?: number }`. |
| `hub/src/components/CapabilityCard.tsx`     | Free-tier badge rendering                     | VERIFIED  | Lines 100-105: conditional `{card.pricing.free_tier !== undefined && card.pricing.free_tier > 0 && ...}`. |

### Key Link Verification

| From                                          | To                      | Via                         | Status   | Details                                                                                     |
|-----------------------------------------------|-------------------------|-----------------------------|----------|---------------------------------------------------------------------------------------------|
| `src/registry/server.ts`                      | `src/types/index.ts`    | CapabilityCard with _internal| VERIFIED | `stripInternal` destructures `_internal` from CapabilityCard type. Import at line 10.       |
| `hub/src/components/CapabilityCard.tsx`       | `hub/src/types.ts`      | HubCard.pricing.free_tier   | VERIFIED | Imports HubCard from `../types.js` (line 8). Accesses `card.pricing.free_tier` at line 100. |

### Requirements Coverage

| Requirement | Source Plan                  | Description                                  | Status         | Evidence                                                         |
|-------------|------------------------------|----------------------------------------------|----------------|------------------------------------------------------------------|
| SCH-02      | 02.25-01-PLAN.md             | (Unknown — not in REQUIREMENTS.md)           | ORPHANED       | ID claimed in PLAN frontmatter; no matching entry in REQUIREMENTS.md |
| SCH-03      | 02.25-01-PLAN.md             | (Unknown — not in REQUIREMENTS.md)           | ORPHANED       | ID claimed in PLAN frontmatter; no matching entry in REQUIREMENTS.md |
| SCH-04      | 02.25-01-PLAN.md             | (Unknown — not in REQUIREMENTS.md)           | ORPHANED       | ID claimed in PLAN frontmatter; no matching entry in REQUIREMENTS.md |
| SCH-05      | 02.25-01-PLAN.md             | (Unknown — not in REQUIREMENTS.md)           | ORPHANED       | ID claimed in PLAN frontmatter; no matching entry in REQUIREMENTS.md |
| SCH-06      | 02.25-01-PLAN.md             | (Unknown — not in REQUIREMENTS.md)           | ORPHANED       | ID claimed in PLAN frontmatter; no matching entry in REQUIREMENTS.md |

**Note:** All five requirement IDs declared in the PLAN frontmatter (SCH-02 through SCH-06) are absent from `.planning/REQUIREMENTS.md`. The file contains only R-001 through R-008, R-013 through R-015, and RRD-01 through RRD-02 series. No SCH-series section exists. The implementation is substantively correct, but traceability to the canonical requirements document is broken.

### Anti-Patterns Found

| File                                         | Line | Pattern                       | Severity | Impact                         |
|----------------------------------------------|------|-------------------------------|----------|-------------------------------|
| None found in modified files                 | —    | —                             | —        | —                             |

Scanned all 8 files listed in SUMMARY.md key-files. No TODO/FIXME/placeholder comments, no empty implementations, no console.log-only handlers.

### Test Suite Status

| Suite                                     | Result  | Notes                                                                                         |
|-------------------------------------------|---------|-----------------------------------------------------------------------------------------------|
| `pnpm test:run` (root, non-hub files)     | PASS    | 197/197 non-hub tests pass (12 test files green)                                              |
| `cd hub && pnpm test`                     | PASS    | 27/27 hub tests pass (6 tests including 2 new badge tests)                                    |
| Hub tests via root `pnpm test:run`        | FAIL    | 6 hub tests fail with "document is not defined" — pre-existing jsdom/environment issue        |

The hub test failures in the root suite are a pre-existing architectural issue documented in the SUMMARY.md. The root vitest config does not configure jsdom for hub test files. This is NOT a regression from this phase — the same 6 tests that existed before this phase also fail under root vitest. Hub tests pass correctly when run via `cd hub && pnpm test` as specified in the PLAN verification spec.

### Human Verification Required

None. All behaviors are programmatically verifiable through code inspection and the test suite.

### Gaps Summary

Two gaps prevent a full "passed" status:

**Gap 1 — Missing CLI test (minor):** The `_internal` stripping in `src/cli/index.ts` is structurally correct (line 321 strips before both JSON and table output paths). However, the CLI integration test file (`src/cli/index.test.ts`) has no test that publishes a card with `_internal` data and then asserts the `--json` discover output is free of the `_internal` key. The other two stripping points (GET /cards, GET /cards/:id) each have dedicated server tests. The CLI stripping is the only untested stripping boundary.

**Gap 2 — Orphaned requirement IDs (process issue):** The PLAN declares `requirements: [SCH-02, SCH-03, SCH-04, SCH-05, SCH-06]` but none of these IDs exist in `.planning/REQUIREMENTS.md`. The implementation delivers exactly what these IDs presumably intend (schema fields, server stripping, CLI stripping, Hub badge), but the requirements traceability chain is broken. Either the SCH-series requirements must be added to REQUIREMENTS.md, or the PLAN must reference existing requirement IDs that cover this functionality. Note that R-001 (Capability Card Schema) and R-003 (CLI Interface) partially cover this ground but were not updated to reflect the new fields.

The core implementation is complete and correct. The functional goal (additive optional fields, server/CLI stripping, Hub badge) is achieved at the code level.

---

_Verified: 2026-03-15T03:15:00Z_
_Verifier: Claude (gsd-verifier)_
