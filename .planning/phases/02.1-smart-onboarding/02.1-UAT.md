---
status: complete
phase: 02.1-smart-onboarding
source: [02.1-01-SUMMARY.md, 02.1-02-SUMMARY.md]
started: 2026-03-14T19:35:00Z
updated: 2026-03-14T19:50:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Build from scratch (`pnpm build`) and run `node dist/cli/index.js --version`. Should print version number without errors.
result: pass

### 2. Init with --no-detect skips detection
expected: Run `AGENTBNB_DIR=$(mktemp -d) node dist/cli/index.js init --no-detect`. Should create config.json and bootstrap credits, but stdout should NOT contain "Detected" or any API key names. No draft cards published.
result: pass

### 3. Init detects API keys from environment
expected: Run `OPENAI_API_KEY=test AGENTBNB_DIR=$(mktemp -d) node dist/cli/index.js init --yes`. Stdout should mention "OpenAI" in a detection summary and auto-publish a draft Capability Card.
result: pass

### 4. Init with --yes auto-publishes without prompts
expected: Run `ELEVENLABS_API_KEY=test OPENAI_API_KEY=test AGENTBNB_DIR=$(mktemp -d) node dist/cli/index.js init --yes`. Should detect multiple API keys and auto-publish draft cards for each.
result: pass

### 5. Init non-TTY without --yes shows skip notice
expected: Run `echo "" | OPENAI_API_KEY=test AGENTBNB_DIR=$(mktemp -d) node dist/cli/index.js init`. Detection should run, cards should NOT be published.
result: pass

### 6. JSON output includes detection fields
expected: Run `OPENAI_API_KEY=test AGENTBNB_DIR=$(mktemp -d) node dist/cli/index.js init --yes --json`. Output should be valid JSON containing `detected_keys` and `published_cards` fields.
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
