---
phase: 02.1-smart-onboarding
verified: 2026-03-14T03:25:00Z
status: passed
score: 10/10 must-haves verified
---

# Phase 2.1: Smart Onboarding Verification Report

**Phase Goal:** Sub-2-minute onboarding -- `agentbnb init` auto-detects API keys, generates draft Capability Cards, and polishes CLI ergonomics to maximize Phase 2 cold start conversion.
**Verified:** 2026-03-14T03:25:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | detectApiKeys() checks env var existence (boolean only, never reads values) | VERIFIED | `onboarding.ts:154` uses `key in process.env` pattern; grep for `process.env[` returns zero matches in source |
| 2 | isPortOpen() and detectOpenPorts() probe local ports via TCP | VERIFIED | `onboarding.ts:165-202` uses `createConnection` with timeout; 3 unit tests verify open/closed/timeout behavior |
| 3 | buildDraftCard() generates valid CapabilityCards from API templates | VERIFIED | `onboarding.ts:211-237` returns full CapabilityCard; test confirms `CapabilityCardSchema.safeParse(card).success === true` |
| 4 | 10 API templates defined (OpenAI, Anthropic, ElevenLabs, Kling, Stability, Replicate, Google, Azure, Cohere, Mistral) | VERIFIED | `KNOWN_API_KEYS` array has 10 entries; `API_TEMPLATES` record has matching entry for each; test verifies 1:1 mapping |
| 5 | Security: no `process.env[` index access patterns in onboarding.ts | VERIFIED | Grep returns zero matches; dedicated test (`security constraint`) reads source and asserts regex `/process\.env\[/` does not match |
| 6 | CLI --yes flag auto-publishes all draft cards non-interactively | VERIFIED | `index.ts:76,129-139` opts.yes triggers insertCard for each draft; integration test confirms OpenAI card published with --yes |
| 7 | CLI --no-detect flag skips detection entirely | VERIFIED | `index.ts:77,106,111` Commander negates --no-detect to opts.detect===false; integration test confirms no "Detected" output and zero cards |
| 8 | Non-TTY guard before interactive detection | VERIFIED | `index.ts:143` checks `process.stdout.isTTY`; non-TTY without --yes prints skip notice; integration test verifies cards not published in non-TTY |
| 9 | Interactive card confirmation flow (readline prompt) | VERIFIED | `index.ts:32-43` confirm() function uses readline; `index.ts:144-159` calls confirm() per draft card in TTY mode |
| 10 | All tests pass (163+) | VERIFIED | `pnpm vitest run` reports 163 passed, 0 failed, 10 test files |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/cli/onboarding.ts` | Detection + card generation functions | VERIFIED | 237 lines, 6 exports (KNOWN_API_KEYS, API_TEMPLATES, detectApiKeys, isPortOpen, detectOpenPorts, buildDraftCard), no stubs |
| `src/cli/onboarding.test.ts` | Unit tests for all functions | VERIFIED | 232 lines, 21 tests covering all functions + security constraint + constant structure |
| `src/cli/index.ts` (modified) | CLI init wired with --yes, --no-detect, onboarding flow | VERIFIED | Imports all 4 onboarding exports, wired into init action with full detection/publish flow |
| `src/cli/index.test.ts` (modified) | Integration tests for init onboarding flags | VERIFIED | 6 new tests in "CLI: init onboarding" describe block covering all flag combinations |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `index.ts` | `onboarding.ts` | `import { detectApiKeys, detectOpenPorts, buildDraftCard, KNOWN_API_KEYS }` | WIRED | Line 14; all 4 imports used in init action (lines 112-127) |
| `index.ts` init action | `registry/store.ts` | `insertCard(db, card)` | WIRED | Lines 134, 150 -- draft cards inserted into SQLite registry |
| `onboarding.ts` | `types/index.ts` | `import type { CapabilityCard, IOSchema }` | WIRED | Line 15; types used in buildDraftCard return type and CardTemplate interface |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| ONB-01 | 02.1-01 | Auto-detect known AI API env vars (existence only) | SATISFIED | detectApiKeys() with `key in process.env` pattern |
| ONB-02 | 02.1-01 | Scan for running agent processes on known ports | SATISFIED | isPortOpen() + detectOpenPorts() with TCP probe |
| ONB-03 | 02.1-01 | Generate draft Capability Card from detected APIs | SATISFIED | buildDraftCard() + 10 API_TEMPLATES |
| ONB-04 | 02.1-02 | Interactive user confirmation before publishing | SATISFIED | confirm() readline prompt in TTY mode |
| ONB-05 | 02.1-02 | CLI ergonomics polish (first-impression output) | SATISFIED | Detection summary output, skip notice for non-TTY |
| ONB-06 | 02.1-02 | --yes flag for non-interactive mode | SATISFIED | --yes auto-publishes all draft cards |
| ONB-07 | 02.1-02 | --no-detect flag to skip detection | SATISFIED | --no-detect prevents detectApiKeys/detectOpenPorts calls |

Note: ONB requirements are referenced in ROADMAP.md and plan frontmatter but not formally defined in REQUIREMENTS.md. All 7 are implemented and tested.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | - | - | - | No anti-patterns detected |

No TODO/FIXME/PLACEHOLDER/HACK comments found in onboarding.ts or index.ts. No empty implementations. No console.log-only handlers.

### Human Verification Required

### 1. Interactive Card Confirmation Flow

**Test:** Run `OPENAI_API_KEY=test agentbnb init --owner demo` in a terminal (TTY)
**Expected:** Prompt "Publish "OpenAI Text Generation"? [y/N]" appears; answering 'y' publishes, 'n' skips
**Why human:** TTY readline interaction cannot be tested via execSync (always non-TTY)

### 2. Detection Output Formatting

**Test:** Run `OPENAI_API_KEY=test ANTHROPIC_API_KEY=test agentbnb init --owner demo --yes` and review stdout
**Expected:** Clean formatted output showing detected keys count, published card names, and standard init summary
**Why human:** Output formatting and readability is a visual/UX judgment

---

_Verified: 2026-03-14T03:25:00Z_
_Verifier: Claude (gsd-verifier)_
