---
phase: 18-readme-visual-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - README.md
autonomous: false
requirements:
  - README-01
  - README-02
  - README-03
  - README-04

must_haves:
  truths:
    - "README has badges including a tests-passing badge at the top"
    - "README displays the hero banner SVG prominently"
    - "README has clear structured sections: What, Install, Quick Start, Architecture, Contributing"
    - "README references the real hub-screenshot.png"
  artifacts:
    - path: "README.md"
      provides: "Visually compelling project README"
      contains: "shields.io"
    - path: "README.md"
      provides: "Hero banner reference"
      contains: "banner.svg"
    - path: "README.md"
      provides: "Structured sections"
      contains: "Contributing"
  key_links:
    - from: "README.md"
      to: "docs/banner.svg"
      via: "img src reference"
      pattern: "docs/banner\\.svg"
    - from: "README.md"
      to: "docs/hub-screenshot.png"
      via: "img src reference"
      pattern: "docs/hub-screenshot\\.png"
---

<objective>
Rewrite README.md with badges, hero banner, structured sections, and screenshot — transforming it from a dense technical document into a visually compelling first impression for GitHub visitors.

Purpose: The README is the first thing people see on GitHub. A polished, well-structured README with visual assets (banner, screenshot, badges) dramatically increases engagement and credibility.

Output: Fully rewritten `README.md` satisfying all four README requirements.
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-readme-visual-overhaul/18-RESEARCH.md
@.planning/phases/18-readme-visual-overhaul/18-01-SUMMARY.md
@README.md
</context>

<interfaces>
<!-- From Plan 01 — the two image assets this plan must reference -->
docs/banner.svg — SVG hero banner (1200x300, dark bg, emerald title, doodle creature)
docs/hub-screenshot.png — Real 1280x800 PNG of Hub Discover page
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with visual overhaul</name>
  <files>README.md</files>
  <action>
Rewrite README.md completely. Read the current README.md first, then write the new version. Preserve ALL existing technical content but reorganize it into the target structure below.

**Target structure (top to bottom):**

```
# AgentBnB

[badge row — 6 badges]

[hero banner — centered img tag]

[one-line tagline — centered italic]

## What Is This?
[Concise explanation of AgentBnB — agent-native, P2P capability sharing]
[The "idle APIs" pitch in 3-4 sentences]
[Link to AGENT-NATIVE-PROTOCOL.md]

## Agent Hub
[centered screenshot img — docs/hub-screenshot.png]
[1-2 sentence description of the Hub]

## Install
[The existing install table — keep as-is]

## Quick Start
[Move up from current position — the 5-command getting started block]
[Keep the autonomy config lines]

## Key Features
[Bulleted feature list — consolidate from current Features section]
[Collapse the verbose Multi-Skill Card JSON, Autonomy Tiers table, Auto-Share detail into brief bullets with links]

## Architecture
[Keep the existing ASCII tree]
[Keep Agent lifecycle and Credit flow subsections]

## Development
[Keep the existing pnpm commands block]

## Contributing
[NEW section — 3-4 lines: "Contributions welcome" + point to AGENT-NATIVE-PROTOCOL.md + "open issues on GitHub"]

## License
[Keep existing — MIT, copyright Cheng Wen Chen]
```

**Badge row specifics (README-01):**
Keep the existing 5 badges from Phase 15 AND add a "tests passing" badge. The 6 badges in order:
1. `[![npm version](https://img.shields.io/npm/v/agentbnb.svg)](https://www.npmjs.com/package/agentbnb)`
2. `[![Tests](https://img.shields.io/badge/tests-passing-brightgreen.svg)](https://github.com/Xiaoher-C/agentbnb)`
3. `[![Node.js](https://img.shields.io/badge/node-%3E%3D20-brightgreen.svg)](https://nodejs.org/)`
4. `[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)`
5. `[![Claude Code Plugin](https://img.shields.io/badge/Claude%20Code-Plugin-orange.svg)](https://github.com/Xiaoher-C/agentbnb)`
6. `[![Agent Skills](https://img.shields.io/badge/Agent%20Skills-Compatible-blue.svg)](https://agentskills.io)`

Use a static "tests-passing" badge (not CI-linked) per research recommendation — CI setup is Phase 19 scope.

**Hero banner placement (README-02):**
```html
<p align="center">
  <img src="docs/banner.svg" alt="AgentBnB — P2P Agent Capability Sharing" width="100%">
</p>
```
Place immediately after the badge row, before the tagline. Use `<p align="center">` because GitHub strips CSS but honors HTML align attributes.

**Hub screenshot placement (README-04):**
```html
<p align="center">
  <img src="docs/hub-screenshot.png" alt="AgentBnB Hub" width="100%">
</p>
```
Place inside the "## Agent Hub" section.

**Section restructuring specifics (README-03):**
- Rename "The Core Idea" to "## What Is This?"
- Move "Quick Start" UP from its current buried position to right after Install
- Collapse these verbose sections into bullet points under "## Key Features":
  - "Multi-Skill Capability Card" (the full JSON example) — condense to 2-3 sentence description
  - "Autonomy Tiers" (the full table) — condense to 1 bullet with brief explanation
  - "Auto-Share + Auto-Request" — condense to 1-2 bullets
- REMOVE these sections entirely (content folded into Key Features or dropped):
  - "Requirements" (move the Node.js 20+ requirement to a one-liner in Install)
  - "Two-Machine Setup" (too detailed for README — could go to docs)
  - "Commands Reference" (too detailed for README — reference CLI help instead)
  - "Examples" (too detailed — mention in Quick Start or Contributing)
  - "OpenClaw Integration" (fold into Key Features as a bullet)
- ADD "## Contributing" section (new)
- Keep `## Architecture`, `## Development`, `## License` largely as-is

**Content to preserve exactly:**
- The ASCII architecture tree
- The agent lifecycle description
- The credit flow description
- The development pnpm commands
- The install table
- The license/copyright

**Content to condense:**
- Multi-Skill Card JSON — replace with 2-sentence description + "See full schema in CLAUDE.md"
- Autonomy Tiers table — replace with brief description of the 3-tier model
- Auto-Share/Auto-Request detail — condense to feature bullets

**Tagline line (centered, italic, below banner):**
```html
<p align="center"><em>The npm for agent capabilities — P2P sharing protocol for AI agents</em></p>
```

Do NOT add horizontal rules (`---`) between every section. Use them sparingly (after the hero area, before License).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('README.md','utf8'); const checks = { badges: f.includes('tests-passing-brightgreen'), banner: f.includes('docs/banner.svg'), screenshot: f.includes('docs/hub-screenshot.png'), what: /## What Is This/.test(f), install: /## Install/.test(f), quickstart: /## Quick Start/.test(f), arch: /## Architecture/.test(f), contrib: /## Contributing/.test(f), license: /## License/.test(f) }; const failed = Object.entries(checks).filter(([k,v]) => !v); if(failed.length) { console.error('MISSING:', failed.map(([k])=>k).join(', ')); process.exit(1); } console.log('README.md structure OK — all sections and references present')"</automated>
  </verify>
  <done>README.md has 6 badges (including tests-passing), hero banner SVG centered at top, hub screenshot in Agent Hub section, structured sections (What Is This, Install, Quick Start, Key Features, Architecture, Development, Contributing, License), no verbose JSON blocks or tables that belonged in docs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify README visual overhaul</name>
  <files>README.md, docs/banner.svg, docs/hub-screenshot.png</files>
  <action>
Human verifies the complete README visual overhaul across all three deliverables: banner SVG, Hub screenshot, and restructured README.
  </action>
  <what-built>Complete README visual overhaul: hero banner SVG, real Hub screenshot, restructured README with badges and clear sections</what-built>
  <how-to-verify>
    1. Open `docs/banner.svg` in a browser — verify dark background, emerald "AgentBnB" title, doodle creature, grid overlay
    2. Open `docs/hub-screenshot.png` — verify it shows the Hub Discover page with dark theme
    3. Open `README.md` in a Markdown previewer (or `code README.md` in VS Code with preview) — verify:
       a. 6 badges visible at top (npm version, tests passing, Node.js, License, Claude Code, Agent Skills)
       b. Hero banner renders below badges
       c. Tagline centered below banner
       d. Sections in order: What Is This, Agent Hub (with screenshot), Install, Quick Start, Key Features, Architecture, Development, Contributing, License
       e. No duplicate content, no broken image references
       f. Contributing section exists
    4. Confirm the verbose JSON (Multi-Skill Card), Autonomy Tiers table, and Commands Reference table are condensed or removed
  </how-to-verify>
  <verify>
    <automated>node -e "console.log('Checkpoint — human verification required')"</automated>
  </verify>
  <done>Human has reviewed and approved the README visual overhaul</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- README.md contains all required sections: What Is This, Install, Quick Start, Architecture, Contributing
- README.md references `docs/banner.svg` and `docs/hub-screenshot.png`
- README.md has 6 badges including the new tests-passing badge
- No broken image references (both image files exist from Plan 01)
- `pnpm test:run` still passes (no regressions — this plan only modifies README.md)
</verification>

<success_criteria>
- First-time GitHub visitor sees: badges, hero banner, tagline, clear "What Is This?" section, screenshot, install table, quick start — all above the fold or within first 2 scrolls
- README is scan-friendly: clear section headers, no walls of JSON or tables
- All four README requirements (README-01 through README-04) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/18-readme-visual-overhaul/18-02-SUMMARY.md`
</output>
