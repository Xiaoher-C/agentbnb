---
phase: 02.3-remote-registry-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/remote-registry.ts
  - src/cli/remote-registry.test.ts
  - src/cli/config.ts
  - src/cli/index.ts
autonomous: true
requirements: [RRD-01]

must_haves:
  truths:
    - "fetchRemoteCards() fetches from GET /cards with forwarded q, level, online, tag params and 5s timeout"
    - "mergeResults() deduplicates by card ID with local winning, interleaves when query present"
    - "discover --registry <url> displays remote cards in table with [remote] source tag"
    - "discover --registry --json includes source field on each card object"
    - "discover uses config.registry as fallback when --registry flag absent"
    - "config set/get commands persist and retrieve registry URL"
    - "Timeout, connection, and invalid URL errors produce actionable messages"
    - "Explicit --registry failure exits 1; config default failure degrades gracefully with local results"
  artifacts:
    - path: "src/cli/remote-registry.ts"
      provides: "fetchRemoteCards(), mergeResults(), error classes"
      exports: ["fetchRemoteCards", "mergeResults", "RegistryTimeoutError", "RegistryConnectionError", "RegistryAuthError"]
    - path: "src/cli/remote-registry.test.ts"
      provides: "Unit tests for fetchRemoteCards and mergeResults"
      min_lines: 80
    - path: "src/cli/config.ts"
      provides: "AgentBnBConfig with optional registry field"
      contains: "registry?: string"
    - path: "src/cli/index.ts"
      provides: "discover --registry/--tag flags, config command, result merging"
      contains: "config"
  key_links:
    - from: "src/cli/index.ts"
      to: "src/cli/remote-registry.ts"
      via: "import fetchRemoteCards, mergeResults"
      pattern: "import.*fetchRemoteCards.*remote-registry"
    - from: "src/cli/index.ts"
      to: "src/cli/config.ts"
      via: "loadConfig().registry for fallback URL"
      pattern: "config\\.registry"
    - from: "src/cli/remote-registry.ts"
      to: "GET /cards endpoint"
      via: "native fetch with AbortController"
      pattern: "fetch.*\\/cards"
---

<objective>
Build the remote registry fetch module, config command, and wire them into the discover command.

Purpose: Enable `agentbnb discover --registry <url>` to query remote registry servers, with `config set registry` for persistent defaults, result merging with dedup, and source tagging.

Output: Working `fetchRemoteCards()` + `mergeResults()` functions with unit tests, `config` CLI subcommand, and updated `discover` command supporting remote + local merged output.
</objective>

<execution_context>
@/Users/leyufounder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leyufounder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-remote-registry-discovery/02.3-CONTEXT.md
@.planning/phases/02.3-remote-registry-discovery/02.3-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/cli/config.ts:
```typescript
export interface AgentBnBConfig {
  owner: string;
  gateway_url: string;
  gateway_port: number;
  db_path: string;
  credit_db_path: string;
  token: string;
}

export function getConfigDir(): string;
export function getConfigPath(): string;
export function loadConfig(): AgentBnBConfig | null;
export function saveConfig(config: AgentBnBConfig): void;
```

From src/types/index.ts:
```typescript
export class AgentBnBError extends Error {
  constructor(message: string, public code: string);
}
```

From src/gateway/client.ts (pattern reference for AbortController + fetch):
```typescript
const controller = new AbortController();
const timer = setTimeout(() => controller.abort(), timeoutMs);
try {
  response = await fetch(url, { signal: controller.signal });
} catch (err) {
  clearTimeout(timer);
  const isTimeout = err instanceof Error && err.name === 'AbortError';
  // classify error
} finally {
  clearTimeout(timer);
}
```

From src/registry/server.ts (GET /cards query params):
- q: string (FTS5 search)
- level: number (1, 2, 3)
- online: boolean
- tag: string
- limit: number (default 20, max 100)
- offset: number

Discover command location: src/cli/index.ts lines 258-354
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create remote-registry module with fetchRemoteCards, mergeResults, and error classes</name>
  <files>src/cli/remote-registry.ts, src/cli/remote-registry.test.ts, src/cli/config.ts</files>
  <behavior>
    - fetchRemoteCards('http://host:7701', { q: 'voice', level: 1 }) builds URL http://host:7701/cards?q=voice&level=1&limit=100
    - fetchRemoteCards returns items array from paginated response { total, limit, offset, items }
    - fetchRemoteCards throws RegistryTimeoutError when AbortController fires after 5s
    - fetchRemoteCards throws RegistryConnectionError on ECONNREFUSED / network failure
    - fetchRemoteCards throws RegistryAuthError on 401/403 response
    - fetchRemoteCards with invalid URL ('not-a-url') throws AgentBnBError with INVALID_REGISTRY_URL code before fetch
    - mergeResults(local, remote, false) deduplicates by id with local winning, returns local-first order
    - mergeResults(local, remote, true) deduplicates by id with local winning, returns interleaved (alternating local/remote)
    - mergeResults tags each card with source: 'local' or 'remote'
  </behavior>
  <action>
    1. Add `registry?: string` field to `AgentBnBConfig` in `src/cli/config.ts` (optional, after `token`).

    2. Create `src/cli/remote-registry.ts` with:
       - Error classes extending `AgentBnBError`:
         - `RegistryTimeoutError(url)` — message: "Registry at {url} did not respond within 5s. Showing local results only." code: REGISTRY_TIMEOUT
         - `RegistryConnectionError(url)` — message: "Cannot reach {url}. Is the registry running? Showing local results only." code: REGISTRY_CONNECTION
         - `RegistryAuthError(url)` — message: "Authentication failed for {url}. Run `agentbnb config set token <your-token>`." code: REGISTRY_AUTH
       - `TaggedCard` type: `CapabilityCard & { source: 'local' | 'remote' }`
       - `fetchRemoteCards(registryUrl: string, params: { q?: string; level?: number; online?: boolean; tag?: string }, timeoutMs = 5_000): Promise<CapabilityCard[]>`
         - Validate URL first: `try { new URL('/cards', registryUrl) } catch { throw new AgentBnBError("Invalid registry URL: {url}", "INVALID_REGISTRY_URL") }`
         - Build URL with URLSearchParams (q, level, online, tag). Always set limit=100.
         - Use AbortController + setTimeout pattern from gateway/client.ts
         - Classify errors: AbortError -> RegistryTimeoutError, other catch -> RegistryConnectionError
         - On 401/403 status -> RegistryAuthError
         - Parse response as `{ items: CapabilityCard[] }`, return items
       - `mergeResults(localCards: CapabilityCard[], remoteCards: CapabilityCard[], hasQuery: boolean): TaggedCard[]`
         - Tag local cards with source: 'local', remote with source: 'remote'
         - Dedup by card.id — local prepended so first occurrence (local) wins via Set filter
         - If hasQuery: interleave (alternate zip from local/remote sublists)
         - If !hasQuery: return deduped array as-is (local first, then remote)

    3. Create `src/cli/remote-registry.test.ts` with unit tests:
       - Use `vi.stubGlobal('fetch', ...)` to mock fetch for fetchRemoteCards tests
       - Test URL building with various param combinations
       - Test successful response parsing
       - Test timeout error (mock fetch that never resolves + short timeoutMs)
       - Test connection error (mock fetch that rejects)
       - Test auth error (mock fetch returning 401)
       - Test invalid URL
       - Test mergeResults dedup (same id in both local and remote)
       - Test mergeResults interleave when hasQuery=true
       - Test mergeResults local-first when hasQuery=false
       - Test source tagging on all cards
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Documents/Github/agentbnb && pnpm vitest run src/cli/remote-registry.test.ts</automated>
  </verify>
  <done>fetchRemoteCards and mergeResults fully tested with mocked fetch. Config interface extended with registry field. All unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire discover --registry, --tag flags and config command into CLI</name>
  <files>src/cli/index.ts</files>
  <action>
    1. Add `config` command to CLI (after the existing commands, before program.parse):
       ```
       const configCmd = program.command('config').description('Get or set AgentBnB configuration values');
       ```
       - `config set <key> <value>`: Validate key against allowlist ['registry']. Load config, set key, saveConfig. Print "Set {key} = {value}".
         - Unknown key: print "Unknown config key: {key}. Valid keys: registry" and exit(1).
         - Not initialized: print "Error: not initialized. Run `agentbnb init` first." and exit(1).
       - `config get <key>`: Load config, print value or "(not set)" if undefined.
         - Not initialized: same error as set.

    2. Add `--registry <url>` and `--tag <tag>` options to the `discover` command (line ~261):
       - `.option('--registry <url>', 'Remote registry URL to query (e.g., http://host:7701)')`
       - `.option('--tag <tag>', 'Filter by metadata tag')`

    3. Update discover action handler (after the --local branch, around line 298):
       - Import `fetchRemoteCards`, `mergeResults`, `TaggedCard`, `RegistryTimeoutError`, `RegistryConnectionError` from `./remote-registry.js`
       - Determine registryUrl: `opts.registry ?? config.registry ?? undefined`
       - Track whether registry was explicit (opts.registry provided) vs from config
       - If registryUrl exists:
         a. Fetch local cards as before (existing code)
         b. Try `fetchRemoteCards(registryUrl, { q: query, level, online: opts.online, tag: opts.tag })`
         c. On success: `mergeResults(localCards, remoteCards, !!query)` -> use merged result
         d. On error:
            - If explicit (`opts.registry`): print error message to stderr and `process.exit(1)`
            - If from config: print warning to stderr, continue with local cards only (tag all as 'local')
       - If no registryUrl: existing behavior (local only), no source column
       - Add `--tag` to local filter: pass tag to local filterCards/searchCards if supported, otherwise skip (local API doesn't have tag filter — apply client-side by checking `card.metadata.tags?.includes(opts.tag)`)

    4. Update table output (only when remote sources involved):
       - When merged results have any 'remote' source, add Source column:
         ```
         col('ID', 16) + '  ' + col('Name', 28) + '  ' + col('Lvl', 3) + '  ' + col('Credits', 7) + '  ' + col('Online', 6) + '  ' + col('Source', 8)
         ```
         Name column narrowed from 32 to 28 to fit Source column.
         Separator: '-'.repeat(80)
       - When local-only (no registry involved): preserve existing table format unchanged (32-wide Name, '-'.repeat(72))
       - JSON output: when source tagging is present, include `source` field in output objects

    5. Ensure `_internal` stripping applies to both local and remote cards before output.

    Important anti-patterns to avoid:
    - Do NOT modify table format for local-only discover (preserves existing tests)
    - Do NOT re-validate remote cards through Zod schema
    - Do NOT add advanced filters (sort, pagination, min_success_rate, max_latency_ms) per user decision
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Documents/Github/agentbnb && pnpm vitest run src/cli/</automated>
  </verify>
  <done>discover --registry shows remote cards with [remote] tag. discover without --registry preserves existing behavior. config set/get works. --tag filters cards. All existing CLI tests still pass. New unit tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/cli/` — all CLI tests pass (existing + new)
- `pnpm vitest run` — full suite green (no regressions)
- `npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
- fetchRemoteCards fetches from remote GET /cards with query params and 5s timeout
- mergeResults deduplicates and tags cards by source
- discover --registry displays merged local+remote results with source column
- discover without --registry preserves existing output format
- config set registry / config get registry works
- --tag flag filters cards by metadata tag
- Error messages match CONTEXT.md specifications exactly
- All existing tests pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-remote-registry-discovery/02.3-01-SUMMARY.md`
</output>
