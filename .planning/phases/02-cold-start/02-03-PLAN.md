---
phase: 02-cold-start
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/cli/index.ts
  - src/cli/index.test.ts
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [R-013, R-014, R-015]

must_haves:
  truths:
    - "Running `agentbnb serve` starts both the gateway (port 7700) and the registry server (port 7701)"
    - "Running `agentbnb serve --registry-port 0` disables the registry server"
    - "Running `agentbnb serve --registry-port 8080` starts registry on port 8080"
    - "Registry server shares the same SQLite database instance as the gateway (no separate process)"
    - "Graceful shutdown stops both servers"
    - "R-013, R-014, R-015 are formally defined in REQUIREMENTS.md"
  artifacts:
    - path: "src/cli/index.ts"
      provides: "Extended serve command with --registry-port flag"
      contains: "registry-port"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Formal R-013, R-014, R-015 requirement definitions"
      contains: "R-013"
  key_links:
    - from: "src/cli/index.ts"
      to: "src/registry/server.ts"
      via: "import { createRegistryServer }"
      pattern: "createRegistryServer"
    - from: "src/cli/index.ts"
      to: "src/gateway/server.ts"
      via: "import { createGatewayServer } (existing)"
      pattern: "createGatewayServer"
---

<objective>
Wire the registry server into the CLI `serve` command so both gateway and registry run in the same process, and formalize Phase 2 requirements in REQUIREMENTS.md. Conclude with human verification of the full end-to-end flow.

Purpose: Make the public registry accessible via the standard `agentbnb serve` command. Complete the planning artifact trail by adding R-013/R-014/R-015 to REQUIREMENTS.md.

Output: Updated CLI with `--registry-port`, updated REQUIREMENTS.md, human-verified end-to-end.
</objective>

<execution_context>
@/Users/leyufounder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leyufounder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cold-start/02-RESEARCH.md

@src/cli/index.ts
@src/registry/server.ts (created in Plan 02)
@src/gateway/server.ts

<interfaces>
<!-- From Plan 02 output -->
From src/registry/server.ts (created by Plan 02):
```typescript
export interface RegistryServerOptions {
  registryDb: Database.Database;
  silent?: boolean;
}
export function createRegistryServer(opts: RegistryServerOptions): FastifyInstance;
```

From src/gateway/server.ts (existing):
```typescript
export function createGatewayServer(opts: GatewayOptions): FastifyInstance;
```

From src/cli/index.ts — existing serve command structure:
```typescript
program
  .command('serve')
  .option('--port <port>', 'Port to listen on (overrides config)')
  .option('--handler-url <url>', 'Local capability handler URL', 'http://localhost:8080')
  .option('--announce', 'Announce this gateway on the local network via mDNS')
  .action(async (opts) => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire registry server into CLI serve command</name>
  <files>src/cli/index.ts, src/cli/index.test.ts</files>
  <behavior>
    - Test 1: serve command accepts --registry-port option (parsed correctly)
    - Test 2: When registry-port > 0, createRegistryServer is called with the shared registryDb
    - Test 3: When registry-port is 0, registry server is not started
  </behavior>
  <action>
    Modify `src/cli/index.ts`:

    1. Add import: `import { createRegistryServer } from '../registry/server.js';`

    2. Extend the `serve` command options:
       - Add `.option('--registry-port <port>', 'Public registry API port (0 to disable)', '7701')`

    3. In the serve action handler:
       - Parse `const registryPort = parseInt(opts.registryPort, 10);`
       - After creating the gateway server (existing code), add:
         ```typescript
         let registryServer: FastifyInstance | null = null;
         if (registryPort > 0) {
           registryServer = createRegistryServer({ registryDb, silent: false });
           await registryServer.listen({ port: registryPort, host: '0.0.0.0' });
           console.log(`Registry API: http://0.0.0.0:${registryPort}/cards`);
         }
         ```
       - The `registryDb` variable is already in scope (opened for the gateway).
       - This ensures both servers share the SAME Database instance (no SQLite WAL lock issues per RESEARCH.md Pitfall 1).

    4. Update `gracefulShutdown()`:
       - Before `await server.close()`, add: `if (registryServer) await registryServer.close();`
       - Keep existing order: stop mDNS announcement first, then close servers, then close DBs.

    5. Update the opts type to include `registryPort: string`.

    Write tests in src/cli/index.test.ts (extend existing if it exists, create if not). Tests can verify option parsing by checking commander's parsed options, or by testing the serve command behavior with mocked servers.
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Documents/Github/agentbnb && pnpm test:run && pnpm typecheck</automated>
  </verify>
  <done>serve command has --registry-port flag defaulting to 7701. Registry server starts on that port sharing the gateway's registryDb. --registry-port 0 disables registry. Graceful shutdown closes both servers. All tests pass, no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Formalize R-013, R-014, R-015 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Append Phase 2 requirements to `.planning/REQUIREMENTS.md` after the existing Phase 1 section:

    ```markdown
    ## Phase 2: Cold Start Requirements

    ### R-013: Web-Based Registry
    **Status**: In Progress (02-02)
    **Priority**: P0

    Public HTTP REST API exposing the capability card registry for external discovery:
    - Separate Fastify server instance (not on the gateway)
    - CORS enabled for browser access
    - GET /health — server status
    - GET /cards — paginated list with filters and search
    - GET /cards/:id — single card by UUID
    - Read-only: no write endpoints

    **Acceptance Criteria**:
    - [ ] GET /cards returns paginated response { total, limit, offset, items }
    - [ ] FTS5 search via ?q= parameter
    - [ ] Filters: level, online, tag, min_success_rate, max_latency_ms
    - [ ] Sort: success_rate (desc), latency (asc)
    - [ ] CORS headers present on all responses
    - [ ] Registry accessible at http://host:7701/cards via `agentbnb serve`

    ### R-014: Reputation System
    **Status**: In Progress (02-01)
    **Priority**: P0

    Per-card reputation tracking updated after each capability execution:
    - Exponentially weighted average (EWA) with alpha=0.1
    - success_rate: 0.0-1.0, updated on success (1) and failure (0)
    - avg_latency_ms: updated with observed execution latency
    - Gateway automatically records reputation after settle (success) and release (failure)
    - First execution bootstraps from undefined to concrete value

    **Acceptance Criteria**:
    - [ ] updateReputation() updates success_rate and avg_latency_ms using EWA
    - [ ] Gateway calls updateReputation() after every capability.execute
    - [ ] Reputation data persists in SQLite (survives restart)
    - [ ] Re-publishing a card preserves existing reputation data

    ### R-015: Capability Card Marketplace
    **Status**: In Progress (02-02)
    **Priority**: P1

    Browse and filter capabilities with reputation-aware sorting:
    - Pagination: limit (default 20, max 100) + offset
    - Tag filtering via metadata.tags
    - Reputation filtering (min_success_rate, max_latency_ms)
    - Sort by success_rate or latency
    - Unrated cards (undefined success_rate) sort after rated cards

    **Acceptance Criteria**:
    - [ ] Pagination returns correct slices with total count
    - [ ] Tag filter matches cards with specified tag
    - [ ] Reputation filters exclude cards below threshold
    - [ ] Sort by success_rate puts highest first, unrated last
    - [ ] Sort by latency puts fastest first, unrated last
    ```

    Read the file first to preserve existing content, then append the Phase 2 section.
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Documents/Github/agentbnb && grep -c "R-013\|R-014\|R-015" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md contains R-013 (Web-Based Registry), R-014 (Reputation System), and R-015 (Marketplace) with acceptance criteria matching the implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify end-to-end Phase 2 flow</name>
  <files></files>
  <action>Present the complete Phase 2 implementation for human verification: reputation system, registry server with marketplace endpoints, and CLI integration.</action>
  <what-built>
    Complete Phase 2 Cold Start implementation:
    1. Reputation system: updateReputation() with EWA, gateway instrumentation
    2. Public registry server: GET /cards with search, filters, pagination, CORS
    3. CLI: `agentbnb serve` now starts both gateway (7700) and registry (7701)
  </what-built>
  <how-to-verify>
    1. Run all tests: `pnpm test:run` — all should pass
    2. Run type check: `pnpm typecheck` — no errors
    3. Start the server: `agentbnb serve --handler-url http://localhost:8080`
       - Confirm output shows both gateway (7700) and registry (7701)
    4. In another terminal:
       - `curl http://localhost:7701/health` — should return {"status":"ok"}
       - `curl http://localhost:7701/cards` — should return {"total":0,"limit":20,"offset":0,"items":[]}
       - Publish a card: `agentbnb publish examples/sample-card.json` (if available)
       - `curl http://localhost:7701/cards` — should now show the published card
       - `curl -I http://localhost:7701/cards` — check for access-control-allow-origin header
    5. Test --registry-port 0: `agentbnb serve --registry-port 0` — should only show gateway, no registry line
  </how-to-verify>
  <verify>Human confirms end-to-end flow works</verify>
  <done>Human approved: tests pass, servers start, curl returns correct responses, CORS headers present.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test:run` — all tests pass
- `pnpm typecheck` — no type errors
- `agentbnb serve` starts both gateway and registry servers
- `curl http://localhost:7701/cards` returns paginated JSON
- CORS headers present on registry responses
- R-013, R-014, R-015 defined in REQUIREMENTS.md
</verification>

<success_criteria>
- CLI serve command starts registry server on port 7701 by default
- --registry-port flag allows changing or disabling (0) the registry port
- Both servers share the same SQLite database instance
- Graceful shutdown cleans up both servers
- REQUIREMENTS.md has formal R-013, R-014, R-015 entries
- Human verified: end-to-end flow works (serve, curl /cards, CORS headers)
</success_criteria>

<output>
After completion, create `.planning/phases/02-cold-start/02-03-SUMMARY.md`
</output>
