---
phase: 01-cli-mvp
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src/cli/index.ts
  - README.md
  - examples/two-agent-demo/agent-a-setup.sh
  - examples/two-agent-demo/agent-b-setup.sh
  - examples/two-agent-demo/demo.sh
  - examples/two-agent-demo/sample-card.json
autonomous: false
requirements: [R-011, R-012]
must_haves:
  truths:
    - "agentbnb init detects LAN IP and uses it for gateway_url (not localhost)"
    - "README explains what AgentBnB is, how to install, and how to use it"
    - "A working two-agent demo exists in examples/ with step-by-step scripts"
    - "External user can follow README to get two agents sharing capabilities"
  artifacts:
    - path: "src/cli/index.ts"
      provides: "init command with LAN IP detection and --host override"
      contains: "networkInterfaces"
    - path: "README.md"
      provides: "Project documentation with install, quickstart, API reference"
      min_lines: 100
    - path: "examples/two-agent-demo/demo.sh"
      provides: "Runnable two-agent demo script"
      min_lines: 20
    - path: "examples/two-agent-demo/sample-card.json"
      provides: "Example Capability Card for demo"
  key_links:
    - from: "README.md"
      to: "examples/two-agent-demo/"
      via: "Documentation references example directory"
      pattern: "examples/two-agent-demo"
    - from: "src/cli/index.ts (init command)"
      to: "node:os networkInterfaces"
      via: "LAN IP detection for gateway_url"
      pattern: "networkInterfaces"
---

<objective>
Fix the init command to use LAN IP for multi-machine communication, then create documentation and examples so external users can successfully install and use AgentBnB.

Purpose: Without LAN IP detection, multi-machine communication fails (localhost is unreachable from other machines). Without docs and examples, external users cannot onboard. R-011 (OpenSpec) is a process adoption documented in README.
Output: Working init with LAN IP, README.md, examples/two-agent-demo/.
</objective>

<execution_context>
@/Users/leyufounder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leyufounder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-cli-mvp/01-RESEARCH.md
@.planning/phases/01-cli-mvp/01-01-SUMMARY.md
@.planning/phases/01-cli-mvp/01-02-SUMMARY.md
@.planning/phases/01-cli-mvp/01-03-SUMMARY.md

<interfaces>
<!-- From RESEARCH.md: LAN IP detection pattern -->
```typescript
import { networkInterfaces } from 'node:os';
function getLanIp(): string {
  const nets = networkInterfaces();
  for (const ifaces of Object.values(nets)) {
    for (const iface of ifaces ?? []) {
      if (iface.family === 'IPv4' && !iface.internal) return iface.address;
    }
  }
  return 'localhost';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix init command to detect and use LAN IP</name>
  <files>src/cli/index.ts</files>
  <action>
    1. Import `networkInterfaces` from `node:os` in `src/cli/index.ts`.
    2. Add `getLanIp()` helper function (per RESEARCH.md pattern): iterate networkInterfaces, find first non-internal IPv4 address, fallback to 'localhost'.
    3. Update the `agentbnb init` command's `gateway_url` from `http://localhost:${port}` to `http://${getLanIp()}:${port}`.
    4. Add `--host <ip>` flag to allow manual override: `gateway_url: http://${opts.host ?? getLanIp()}:${port}`.
    5. Print the detected IP in init output: `  Gateway: http://{ip}:{port}`.
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Documents/Github/agentbnb && pnpm vitest run src/cli/ --reporter=verbose && pnpm typecheck</automated>
  </verify>
  <done>agentbnb init uses LAN IP by default, --host flag available for override, all CLI tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create README and two-agent demo examples</name>
  <files>README.md, examples/two-agent-demo/sample-card.json, examples/two-agent-demo/agent-a-setup.sh, examples/two-agent-demo/agent-b-setup.sh, examples/two-agent-demo/demo.sh</files>
  <action>
    1. Create `README.md` with these sections:
       - **AgentBnB** — one-line description + tagline ("Airbnb for AI agent pipelines")
       - **Features** — bullet list: Capability Cards, P2P discovery, credit system, CLI
       - **Quick Start** — `npm install -g agentbnb` then `agentbnb init --owner alice`
       - **Two-Machine Setup** — step-by-step: init on machine A, init on machine B, connect, publish, discover, request
       - **Commands Reference** — table of all commands (init, publish, discover, request, status, serve, connect, peers) with brief description and key flags
       - **Capability Card Format** — JSON example with spec_version: 1.0
       - **Architecture** — brief explanation of registry, gateway, credit system, discovery
       - **mDNS Discovery** — how --announce and --local work, LAN-only caveat
       - **Development** — pnpm install, pnpm test:run, pnpm build
       - **Requirements** — Node 20+, note about better-sqlite3 prebuilt binaries (per RESEARCH.md Pitfall 1)
       - **License** — MIT
       - Note for R-011: Include a brief "Spec-Driven Development" section mentioning that AgentBnB's API contracts are authored following the OpenSpec SDD methodology. This is the "OpenSpec integration" — a development process adoption, not a runtime dependency.

    2. Create `examples/two-agent-demo/sample-card.json`:
       - A valid Capability Card with spec_version: 1.0
       - Name: "text-summarizer", Level 1, credits_per_call: 5
       - Simple text input, text output

    3. Create `examples/two-agent-demo/agent-a-setup.sh`:
       - `#!/bin/bash` with set -e
       - `agentbnb init --owner alice --port 7700`
       - `agentbnb publish sample-card.json`
       - `agentbnb serve --announce`
       - Comments explaining each step

    4. Create `examples/two-agent-demo/agent-b-setup.sh`:
       - `#!/bin/bash` with set -e
       - `agentbnb init --owner bob --port 7701`
       - `agentbnb discover --local` (find alice via mDNS)
       - `agentbnb connect alice http://<alice-ip>:7700 <alice-token>`
       - Comments noting user must replace placeholder values

    5. Create `examples/two-agent-demo/demo.sh`:
       - Single-machine demo that starts both agents in the same process for quick testing
       - Sets AGENTBNB_DIR to temp dirs for isolation
       - Publishes a card on agent A, discovers from agent B perspective
       - Uses `agentbnb connect` for the cross-agent request
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Documents/Github/agentbnb && test -f README.md && test -f examples/two-agent-demo/sample-card.json && test -f examples/two-agent-demo/demo.sh && test -f examples/two-agent-demo/agent-a-setup.sh && test -f examples/two-agent-demo/agent-b-setup.sh && echo "All files exist"</automated>
  </verify>
  <done>README.md exists with all sections, examples directory has working demo scripts with sample-card.json</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Phase 1 CLI MVP</name>
  <files>README.md, examples/two-agent-demo/demo.sh</files>
  <action>
    Human verifies the complete Phase 1 CLI MVP: npm-ready package with spec v1.0, mDNS discovery, peer management, LAN IP detection, documentation, and examples.

    Steps to verify:
    1. Run `pnpm test:run` — all tests pass (Phase 0 + Phase 1)
    2. Run `pnpm build` — dist/ created successfully
    3. Run `node dist/cli/index.js --version` — prints 1.0.0
    4. Run `agentbnb init --owner test-user` — check that gateway_url uses LAN IP (not localhost)
    5. Run `agentbnb connect peer1 http://example.com:7700 sometoken` — peer saved
    6. Run `agentbnb peers` — shows peer1
    7. Review README.md — clear, accurate, covers all commands
    8. Review examples/two-agent-demo/ — scripts make sense
    9. (Optional) Run demo.sh to verify two-agent flow

    Resume signal: Type "approved" or describe issues.
  </action>
  <verify>Human approves all 9 verification steps above</verify>
  <done>Human approves: tests pass, build works, CLI commands functional, docs complete, examples runnable</done>
</task>

</tasks>

<verification>
- `pnpm test:run` — full suite green
- `pnpm build` — succeeds
- `pnpm typecheck` — clean
- README.md covers all commands and features
- examples/ has runnable demo scripts
- init detects LAN IP correctly
</verification>

<success_criteria>
- External user can follow README to install, init, connect, and request between two machines
- Documentation covers all Phase 1 features including mDNS caveats
- Demo scripts are executable and demonstrate the full flow
- OpenSpec SDD process adoption is documented (R-011)
- Human verification confirms the complete Phase 1 MVP is ready
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-mvp/01-04-SUMMARY.md`
</output>
