---
phase: 11-repo-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "CLAUDE.md Current State section lists v1.1 (8 phases, 24 plans, 302+ tests), v2.0 (5 phases, 12 plans), and v2.1 (3 phases, ~10 plans) with shipped dates"
    - "CLAUDE.md Architecture tree includes all current directories including hub/src/components with new components (CardModal, StatsBar, StatusDot, etc.) and skills/agentbnb with new files (bootstrap.ts, install.sh, HEARTBEAT.rules.md)"
    - "CLAUDE.md references AGENT-NATIVE-PROTOCOL.md as the design bible and states the agent-first philosophy prominently"
    - "CLAUDE.md has no stale references to 'Phase 0 Dogfood' as current, or 'pre-launch preparation' as current phase"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate project context file for Claude Code sessions"
      contains: "v2.1"
  key_links:
    - from: "CLAUDE.md"
      to: "AGENT-NATIVE-PROTOCOL.md"
      via: "markdown link in Project Overview"
      pattern: "AGENT-NATIVE-PROTOCOL\\.md"
---

<objective>
Update CLAUDE.md to accurately reflect the full project state after v1.1, v2.0, and v2.1 milestones.

Purpose: CLAUDE.md is the primary context file that Claude Code reads at the start of every session. Stale content causes Claude to make incorrect assumptions about project state, architecture, and conventions. Updating it ensures future sessions start with accurate context.

Output: Updated CLAUDE.md reflecting all milestones, current architecture, and v2.1 features.
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md to reflect v1.1 + v2.0 + v2.1 reality</name>
  <files>CLAUDE.md</files>
  <action>
Read the current CLAUDE.md, then rewrite it with the following specific changes:

**Current State section — replace entirely with:**
- v1.1 Milestone: 8/8 phases complete, 24 plans, 302+ tests — shipped 2026-03-15
- v2.0 Milestone: 5/5 phases complete (Phases 4-8), 12 plans — shipped 2026-03-15
- v2.1 Milestone: 3/3 phases complete (Phases 9-11), ~10 plans — shipped 2026-03-16
- Current phase: v2.1 complete. Repo ready for public launch.

**Architecture tree — update to include:**
- hub/src/components/ should list: CapabilityCard, CardGrid, CardModal, CategoryChip, EmptyState, ErrorState, LevelBadge, LoginForm, OwnerDashboard, RequestHistory, SearchFilter, SharePage, SkeletonCard, StatsBar, StatusDot (the v2.1 Hub UI redesign components)
- hub/src/components/ line annotation: "CapabilityCard, CardGrid, CardModal, StatsBar, SearchFilter, etc."
- skills/agentbnb/ should list: SKILL.md, bootstrap.ts, install.sh, HEARTBEAT.rules.md, gateway.ts, auto-share.ts, auto-request.ts, credit-mgr.ts, bootstrap.test.ts
- skills/agentbnb/ line annotation: update to include "bootstrap.ts (activate/deactivate entry point), install.sh, HEARTBEAT.rules.md"

**Hub tech stack line — update to:**
- Hub: React 18 + Vite + Tailwind CSS (premium dark SaaS theme, served at `/hub`)

**Important Context section — update to:**
- Remove "v1.1 + v2.0 complete. Currently in pre-launch preparation."
- Replace with: "v1.1 + v2.0 + v2.1 complete. Project is launch-ready."
- Add: "v2.1 added: premium Hub UI (dark theme, modal overlays, count-up animations), ClaWHub installable skill (single activate() entry point), repo documentation updates"
- Keep all other items (founder, agent-first philosophy, hub description, name explanation)

**Do NOT change:**
- Project Overview section (it's already accurate with AGENT-NATIVE-PROTOCOL.md link)
- Tech Stack section (except Hub line)
- Capability Card Schema v2.0 section (already accurate)
- Agent Autonomy Model section (already accurate)
- OpenClaw Integration section (already accurate)
- Coding Conventions section (already accurate)
- GSD Integration section (already accurate)

Preserve the overall structure and formatting conventions (## headers, bullet lists, code blocks).
  </action>
  <verify>
    <automated>grep -c "v2.1" CLAUDE.md | xargs test 1 -le && grep "shipped 2026-03-16" CLAUDE.md && grep "CardModal" CLAUDE.md && grep "bootstrap.ts" CLAUDE.md && grep "launch-ready" CLAUDE.md && echo "PASS"</automated>
  </verify>
  <done>CLAUDE.md contains: v2.1 milestone with shipped date, updated architecture tree with CardModal/StatsBar/bootstrap.ts/install.sh, "launch-ready" status, no stale "pre-launch preparation" references. All existing accurate sections preserved unchanged.</done>
</task>

</tasks>

<verification>
- `grep "v2.1" CLAUDE.md` returns at least one match
- `grep "302+" CLAUDE.md` confirms test count preserved
- `grep "pre-launch preparation" CLAUDE.md` returns NO matches (stale reference removed)
- `grep "AGENT-NATIVE-PROTOCOL" CLAUDE.md` returns at least one match (link preserved)
- `grep "CardModal" CLAUDE.md` returns match (new Hub component listed)
- `grep "bootstrap.ts" CLAUDE.md` returns match (ClaWHub entry point listed)
</verification>

<success_criteria>
CLAUDE.md accurately describes all three milestones (v1.1, v2.0, v2.1), lists the current architecture including all v2.1 additions, and contains no stale references to outdated project state. A new Claude Code session reading this file gets an accurate picture of the project.
</success_criteria>

<output>
After completion, create `.planning/phases/11-repo-housekeeping/11-01-SUMMARY.md`
</output>
