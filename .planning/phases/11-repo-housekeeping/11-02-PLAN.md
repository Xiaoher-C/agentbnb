---
phase: 11-repo-housekeeping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "README.md opens with a compelling tagline about agent idle capacity and autonomous trading"
    - "README.md contains a multi-skill Capability Card JSON example showing skills[] array with at least 2 skills"
    - "README.md explains all 3 autonomy tiers (Tier 1 auto, Tier 2 notify, Tier 3 ask) with CLI config commands"
    - "README.md explains auto-share (idle rate > 70% triggers) and auto-request (capability gap detection + peer scoring)"
    - "README.md references the Hub screenshot and describes the premium dark UI"
    - "A new visitor reading only the README understands what AgentBnB does, how the economics work, and how to get started in under 2 minutes"
  artifacts:
    - path: "README.md"
      provides: "Public-facing project README for GitHub"
      contains: "skills"
  key_links:
    - from: "README.md"
      to: "AGENT-NATIVE-PROTOCOL.md"
      via: "markdown link in design philosophy section"
      pattern: "AGENT-NATIVE-PROTOCOL\\.md"
---

<objective>
Rewrite README.md as a compelling, comprehensive public-facing README that communicates AgentBnB's value proposition, architecture, and usage in under 2 minutes of reading.

Purpose: The README is the first thing a visitor sees on GitHub. It must communicate the core insight (agents trading idle capacity), show the multi-skill card schema, explain autonomy tiers, and make the project feel launch-ready. This is a recruiting tool — first impression determines whether someone stars the repo or leaves.

Output: Rewritten README.md with new tagline, architecture story, and all v2.1 features.
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@v2.1-milestone.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for public launch</name>
  <files>README.md</files>
  <action>
Read the current README.md, then rewrite it completely with the following structure and content:

**Title and tagline:**
```
# AgentBnB

**Your agent has idle APIs. It knows. It wants to trade them.**
```

Keep the existing npm/node/license badges below the title.

**The Core Idea section — rewrite with stronger narrative:**
- Lead with: "The user of AgentBnB is not the human. The user is the agent."
- Explain: idle API subscriptions (ElevenLabs at 90% unused, GPT-4 at 70% unused) = waste. Sharing = earn credits. Credits = access capabilities you lack.
- The human says "Yes" once. The agent handles everything after that.
- Include the existing ASCII flow diagram (idle -> share -> earn -> spend -> smarter)
- Link to AGENT-NATIVE-PROTOCOL.md: "Read the full design philosophy in [AGENT-NATIVE-PROTOCOL.md](AGENT-NATIVE-PROTOCOL.md)"

**Hub Screenshot section (NEW):**
- Add a section titled "## The Hub" or "## Agent Hub"
- Include a placeholder image reference: `![AgentBnB Hub](docs/hub-screenshot.png)`
- Add text: "The Hub is a premium dark SaaS dashboard at `/hub` — browse capabilities, monitor your agent, manage sharing. Dark #08080C background, emerald #10B981 accent, ambient glow, modal card overlays, count-up animations."
- Note: "Screenshot coming soon" as alt-text if preferred — the screenshot file itself will be added later

**Features section — keep existing list but update:**
- Keep all current bullet points (they are accurate)
- Add: "Premium Hub UI — Dark SaaS dashboard with ambient glow, modal overlays, and count-up animations"
- Add: "One-Command Install — `openclaw install agentbnb` and your agent is live on the network"

**Multi-Skill Capability Card section — keep the existing JSON example.** It is already good with the 2-skill example (TTS + Video Gen). No changes needed.

**Autonomy Tiers section — keep existing.** Already accurate with table + CLI commands + safe-by-default explanation.

**Auto-Share + Auto-Request section — keep existing.** Already accurate.

**OpenClaw Integration section — update to emphasize the new one-command install:**
- Add mention of `activate()` entry point from bootstrap.ts
- Add mention that SKILL.md contains agent-executable instructions (not human docs)
- Keep existing CLI commands

**Quick Start section — keep existing.** Already accurate.

**Two-Machine Setup section — keep existing.** Already accurate.

**Architecture section — keep existing ASCII diagram and credit flow.** Already accurate.

**Development section — keep existing.** Already accurate.

**Commands Reference section — keep existing.** Already accurate.

**Examples section — keep existing.** Already accurate.

**License section — update to:**
```
MIT -- see [LICENSE](LICENSE)

(c) 2026 Cheng Wen Chen
```

**Footer — update to:**
```
*Built by Cheng Wen Chen. AgentBnB is the npm for agent capabilities -- open source, agent-native, no lock-in.*
```

Remove any reference to "Developed by" and ensure it says "Built by Cheng Wen Chen" (not any company name).

**Important:** The README should NOT mention phase numbers, plan counts, or internal GSD workflow details. It is a public-facing document, not a project status page.
  </action>
  <verify>
    <automated>grep "idle APIs" README.md && grep "AGENT-NATIVE-PROTOCOL" README.md && grep "skills" README.md && grep "Tier 1" README.md && grep "Tier 3" README.md && grep "Cheng Wen Chen" README.md && grep "hub-screenshot\|Hub" README.md && echo "PASS"</automated>
  </verify>
  <done>README.md has: new tagline about idle APIs, AGENT-NATIVE-PROTOCOL.md link, multi-skill JSON example, autonomy tiers explanation, auto-share/auto-request sections, Hub screenshot reference, OpenClaw one-command install, "Built by Cheng Wen Chen" attribution. No internal GSD references. A GitHub visitor understands the project value proposition within 2 minutes.</done>
</task>

</tasks>

<verification>
- `grep "idle APIs" README.md` returns match (new tagline)
- `grep "AGENT-NATIVE-PROTOCOL" README.md` returns match (design bible linked)
- `grep "skills" README.md` returns match (multi-skill cards)
- `grep "Tier 1" README.md` and `grep "Tier 3" README.md` return matches (autonomy tiers)
- `grep "auto-share\|Auto-Share" README.md` returns match
- `grep "hub-screenshot\|Hub" README.md` returns match (Hub reference)
- `grep "Cheng Wen Chen" README.md` returns match (correct attribution)
- No `grep "Phase 0\|Dogfood\|GSD" README.md` matches (internal references removed)
</verification>

<success_criteria>
README.md is a compelling, complete public-facing document. A new GitHub visitor reads it and understands: (1) what AgentBnB does (P2P agent capability sharing), (2) the core economic model (idle = waste, share = earn, credits = access), (3) the autonomy system (3 tiers, safe by default), (4) how to get started (quick start + CLI), (5) the Hub exists as a visual dashboard — all within 2 minutes.
</success_criteria>

<output>
After completion, create `.planning/phases/11-repo-housekeeping/11-02-SUMMARY.md`
</output>
