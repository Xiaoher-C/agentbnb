---
phase: 20-conductor-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/conductor/types.ts
  - src/conductor/task-decomposer.ts
  - src/conductor/task-decomposer.test.ts
  - src/conductor/card.ts
  - src/conductor/card.test.ts
autonomous: true
requirements: [COND-01, COND-04]
must_haves:
  truths:
    - "TaskDecomposer returns SubTask[] with correct dependencies for 'video production' keyword"
    - "TaskDecomposer returns SubTask[] for 'deep analysis' keyword"
    - "TaskDecomposer returns SubTask[] for 'content generation' keyword"
    - "TaskDecomposer returns empty array for unrecognized task descriptions"
    - "Conductor card is a valid CapabilityCardV2 with orchestrate and plan skills"
    - "Conductor card registers in SQLite via insertCard()"
  artifacts:
    - path: "src/conductor/types.ts"
      provides: "SubTask, MatchResult, ExecutionBudget interfaces shared across conductor"
      exports: ["SubTask", "MatchResult", "ExecutionBudget", "OrchestrationResult"]
    - path: "src/conductor/task-decomposer.ts"
      provides: "decompose() function with 3 hardcoded templates"
      exports: ["decompose", "TEMPLATES"]
    - path: "src/conductor/task-decomposer.test.ts"
      provides: "Tests for all 3 templates + unknown task fallback"
      min_lines: 50
    - path: "src/conductor/card.ts"
      provides: "buildConductorCard() and registerConductorCard() functions"
      exports: ["buildConductorCard", "registerConductorCard"]
    - path: "src/conductor/card.test.ts"
      provides: "Tests for card schema validation and registration"
      min_lines: 30
  key_links:
    - from: "src/conductor/task-decomposer.ts"
      to: "src/conductor/types.ts"
      via: "imports SubTask"
      pattern: "import.*SubTask.*from.*types"
    - from: "src/conductor/card.ts"
      to: "src/types/index.ts"
      via: "uses CapabilityCardV2Schema for validation"
      pattern: "CapabilityCardV2Schema"
    - from: "src/conductor/card.ts"
      to: "src/registry/store.ts"
      via: "calls insertCard() for registration"
      pattern: "insertCard"
---

<objective>
Create the Conductor's shared types, TaskDecomposer (hardcoded template-based task decomposition), and Conductor Card registration.

Purpose: Establish the type contracts for all Conductor components and implement the two components with zero dependencies on other conductor modules — TaskDecomposer (COND-01) is a pure function and Card Registration (COND-04) uses existing registry infrastructure.

Output: src/conductor/ directory with types.ts, task-decomposer.ts, card.ts, and their tests.
</objective>

<execution_context>
@/Users/leyufounder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leyufounder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/index.ts:
```typescript
export type CapabilityCard = z.infer<typeof CapabilityCardSchema>;
export type CapabilityCardV2 = z.infer<typeof CapabilityCardV2Schema>;
export type Skill = z.infer<typeof SkillSchema>;
export class AgentBnBError extends Error {
  constructor(message: string, public code: string);
}

// CapabilityCardV2Schema requires:
// spec_version: '2.0', id: UUID, owner: string, agent_name: string,
// skills: Skill[] (min 1), availability: { online: boolean, schedule?: string }
```

From src/registry/store.ts:
```typescript
export function openDatabase(path = ':memory:'): Database.Database;
export function runMigrations(db: Database.Database): void;
export function insertCard(db: Database.Database, card: CapabilityCard): void;
export function getCard(db: Database.Database, id: string): CapabilityCard | null;
```

From src/autonomy/auto-request.ts (Candidate/ScoredPeer pattern to follow):
```typescript
export interface Candidate {
  card: CapabilityCard;
  cost: number;
  skillId: string | undefined;
}
export interface ScoredPeer extends Candidate {
  rawScore: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conductor types + TaskDecomposer with 3 hardcoded templates</name>
  <files>src/conductor/types.ts, src/conductor/task-decomposer.ts, src/conductor/task-decomposer.test.ts</files>
  <behavior>
    - decompose("Make a 30-second product demo video") returns 4 SubTasks: script → voiceover → video → composite, with correct depends_on chains
    - decompose("Analyze AAPL stock trends in depth") returns 4 SubTasks: research → analyze → summarize → format
    - decompose("Write a blog post about AI agents") returns 4 SubTasks: outline → draft → review → finalize
    - decompose("Something totally unrelated") returns empty SubTask[] (no matching template)
    - All SubTasks have id, description, required_capability, params, depends_on, estimated_credits
    - depends_on correctly models the DAG (e.g., video-production steps 2+3 both depend on step 1, step 4 depends on 2+3)
  </behavior>
  <action>
    1. Create `src/conductor/types.ts` with shared interfaces:
       - `SubTask`: { id: string, description: string, required_capability: string, params: Record<string, unknown>, depends_on: string[], estimated_credits: number }
       - `MatchResult`: { subtask_id: string, selected_agent: string, selected_skill: string, score: number, credits: number, alternatives: Array<{ agent: string, skill: string, score: number, credits: number }> }
       - `ExecutionBudget`: { estimated_total: number, max_budget: number, orchestration_fee: number, per_task_spending: Map<string, number>, requires_approval: boolean }
       - `OrchestrationResult`: { success: boolean, results: Map<string, unknown>, total_credits: number, latency_ms: number, errors?: string[] }

    2. Create `src/conductor/task-decomposer.ts`:
       - Export `TEMPLATES` — a readonly record of 3 template objects:
         - `video-production`: keywords ["video", "demo", "clip", "animation"], steps: script(text_gen) → voiceover(tts) → video(video_gen) → composite(video_edit). Steps 2+3 both depend_on [1], step 4 depends_on [2,3].
         - `deep-analysis`: keywords ["analyze", "analysis", "research", "report", "evaluate"], steps: research(web_search) → analyze(text_gen) → summarize(text_gen) → format(text_gen). Sequential chain.
         - `content-generation`: keywords ["write", "blog", "article", "content", "post", "essay"], steps: outline(text_gen) → draft(text_gen) → review(text_gen) → finalize(text_gen). Sequential chain.
       - Export `decompose(task: string, availableCapabilities?: string[]): SubTask[]`:
         - Lowercase the task string
         - For each template, check if ANY keyword appears in the task
         - Return first matching template's steps as SubTask[], with UUIDs as ids
         - If no template matches, return []
         - `availableCapabilities` param reserved for future filtering (unused in MVP)

    3. Create `src/conductor/task-decomposer.test.ts` — write tests FIRST (RED), then verify implementation passes (GREEN).
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Github/agentbnb && npx vitest run src/conductor/task-decomposer.test.ts</automated>
  </verify>
  <done>TaskDecomposer decomposes tasks into SubTask[] via 3 hardcoded templates. All tests pass. Types exported for downstream use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conductor Card builder and registration</name>
  <files>src/conductor/card.ts, src/conductor/card.test.ts</files>
  <behavior>
    - buildConductorCard() returns a valid CapabilityCardV2 object
    - Card has owner "agentbnb-conductor", agent_name "AgentBnB Conductor", level 3
    - Card has exactly 2 skills: "orchestrate" (5 cr) and "plan" (1 cr)
    - Card passes CapabilityCardV2Schema.parse() without errors
    - registerConductorCard(db) inserts the card and it can be retrieved via getCard()
    - registerConductorCard(db) is idempotent (calling twice does not throw)
  </behavior>
  <action>
    1. Create `src/conductor/card.ts`:
       - `CONDUCTOR_OWNER = 'agentbnb-conductor'` (exported constant)
       - `buildConductorCard(): CapabilityCardV2` — constructs the Conductor's capability card:
         - spec_version: '2.0'
         - id: deterministic UUID (use `crypto.randomUUID()` but cache it, or use a fixed UUID for the conductor since it is a singleton)
         - owner: CONDUCTOR_OWNER
         - agent_name: 'AgentBnB Conductor'
         - skills: [
             { id: 'orchestrate', name: 'Task Orchestration', description: 'Decomposes complex tasks and coordinates multi-agent execution', level: 3, inputs: [{ name: 'task', type: 'text', description: 'Natural language task description' }], outputs: [{ name: 'result', type: 'json', description: 'Aggregated execution results' }], pricing: { credits_per_call: 5 } },
             { id: 'plan', name: 'Execution Planning', description: 'Returns an execution plan with cost estimate without executing', level: 1, inputs: [{ name: 'task', type: 'text', description: 'Natural language task description' }], outputs: [{ name: 'plan', type: 'json', description: 'Execution plan with cost breakdown' }], pricing: { credits_per_call: 1 } }
           ]
         - availability: { online: true }
       - Validate with `CapabilityCardV2Schema.parse(card)` before returning
       - `registerConductorCard(db: Database.Database): CapabilityCardV2`:
         - Build card via buildConductorCard()
         - Check if card already exists via getCard(db, card.id)
         - If exists, call updateCard(db, card.id, card as unknown as Partial<CapabilityCard>, CONDUCTOR_OWNER) — note: insertCard accepts CapabilityCard v1, so for v2 cards, store as JSON data directly using db.prepare('INSERT OR REPLACE INTO capability_cards ...').run(). Look at how insertCard works in store.ts and follow the same pattern but for v2 schema.
         - Return the card

    2. Create `src/conductor/card.test.ts` — write tests FIRST (RED), then verify implementation passes (GREEN).
       - Test buildConductorCard() returns valid schema
       - Test registerConductorCard() with in-memory DB (openDatabase + runMigrations)
       - Test idempotency
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Github/agentbnb && npx vitest run src/conductor/card.test.ts</automated>
  </verify>
  <done>Conductor card builds, validates against CapabilityCardV2Schema, and registers in SQLite. Calling twice is idempotent. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/leyufounder/Github/agentbnb && npx vitest run src/conductor/
```
All conductor tests pass. Types are exported and importable.
</verification>

<success_criteria>
- TaskDecomposer.decompose() returns correct SubTask[] for all 3 templates
- Unknown tasks return empty array
- Conductor card validates against CapabilityCardV2Schema
- Card registers in SQLite and is retrievable
- All tests pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/20-conductor-core/20-01-SUMMARY.md`
</output>
