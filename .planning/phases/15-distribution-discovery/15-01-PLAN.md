---
phase: 15-distribution-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - plugins/agentbnb-network/.claude-plugin/plugin.json
  - plugins/agentbnb-network/skills/agentbnb-network/SKILL.md
  - skills/agentbnb/SKILL.md
  - package.json
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03

must_haves:
  truths:
    - "The Claude Code marketplace file references the agentbnb-network plugin at the correct relative path"
    - "The plugin manifest declares name, version 2.2.0, author, and license"
    - "The plugin SKILL.md guides a Claude Code user through installing and configuring AgentBnB"
    - "The existing skills/agentbnb/SKILL.md uses AgentSkills-standard frontmatter with metadata map"
    - "package.json version matches the plugin version at 2.2.0"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Claude Code marketplace catalog entry"
      contains: "agentbnb-network"
    - path: "plugins/agentbnb-network/.claude-plugin/plugin.json"
      provides: "Plugin manifest with version, author, license"
      contains: "2.2.0"
    - path: "plugins/agentbnb-network/skills/agentbnb-network/SKILL.md"
      provides: "Claude Code plugin skill instructions for human developers"
      contains: "agentbnb-network"
    - path: "skills/agentbnb/SKILL.md"
      provides: "Updated AgentSkills-standard frontmatter"
      contains: "metadata:"
    - path: "package.json"
      provides: "Version bump to 2.2.0"
      contains: "2.2.0"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: "plugins/agentbnb-network/"
      via: "source path in plugins array"
      pattern: "./plugins/agentbnb-network"
    - from: "plugins/agentbnb-network/.claude-plugin/plugin.json"
      to: "plugins/agentbnb-network/skills/agentbnb-network/SKILL.md"
      via: "Claude Code auto-discovers skills/ directory at plugin root"
      pattern: "agentbnb-network"
---

<objective>
Create the Claude Code plugin package and update SKILL.md to AgentSkills standard.

Purpose: Makes AgentBnB installable via the Claude Code plugin marketplace system and discoverable by any AgentSkills-compatible tool through standardized frontmatter.

Output: marketplace.json, plugin.json, plugin SKILL.md, updated skills/agentbnb/SKILL.md, version-bumped package.json
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-distribution-discovery/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Claude Code plugin package</name>
  <files>.claude-plugin/marketplace.json, plugins/agentbnb-network/.claude-plugin/plugin.json, plugins/agentbnb-network/skills/agentbnb-network/SKILL.md</files>
  <action>
Create three files forming the complete Claude Code plugin package.

**1. `.claude-plugin/marketplace.json`** — Marketplace catalog at repo root:
```json
{
  "name": "agentbnb",
  "owner": {
    "name": "Cheng Wen Chen",
    "email": "chengwen@agentbnb.dev"
  },
  "plugins": [
    {
      "name": "agentbnb-network",
      "source": "./plugins/agentbnb-network",
      "description": "Connect your agent to the AgentBnB P2P capability sharing network — earn credits by sharing idle APIs, spend credits to request capabilities from peers"
    }
  ]
}
```

Do NOT set `version` in the marketplace entry — version discipline says plugin.json only.

**2. `plugins/agentbnb-network/.claude-plugin/plugin.json`** — Plugin manifest:
```json
{
  "name": "agentbnb-network",
  "description": "Connect your agent to the AgentBnB P2P capability sharing network — earn credits by sharing idle APIs, spend credits to request capabilities from peers",
  "version": "2.2.0",
  "author": {
    "name": "Cheng Wen Chen",
    "url": "https://github.com/Xiaoher-C/agentbnb"
  },
  "homepage": "https://agentbnb.dev",
  "repository": "https://github.com/Xiaoher-C/agentbnb",
  "license": "MIT",
  "keywords": ["agent", "capability-sharing", "p2p", "credits", "ai-agent", "agentbnb"]
}
```

**3. `plugins/agentbnb-network/skills/agentbnb-network/SKILL.md`** — Plugin skill for Claude Code users (human developers, NOT agents):

Frontmatter:
```yaml
---
name: agentbnb-network
description: Connect your agent to the AgentBnB P2P capability sharing network. Use when setting up AgentBnB, publishing capability cards, managing credits, or requesting capabilities from peer agents.
disable-model-invocation: true
---
```

Body content should guide a human Claude Code user through:
1. Prerequisites (Node.js >= 20, pnpm)
2. Installing AgentBnB globally (`pnpm add -g agentbnb`)
3. Initializing agent identity (`agentbnb init --owner <name>`)
4. Publishing a capability card (`agentbnb publish <card.json>`)
5. Starting the gateway (`agentbnb serve --announce`)
6. Configuring autonomy tiers (`agentbnb config set tier1/tier2/reserve`)
7. OpenClaw integration (`agentbnb openclaw sync`)
8. Viewing the Hub at http://localhost:7700/hub
9. Key CLI commands reference table

Keep it concise and action-oriented. This is a task skill (manual invocation only). Include bash code blocks for all commands.

CRITICAL: `.claude-plugin/` contains ONLY plugin.json. The `skills/` directory is at the plugin root (`plugins/agentbnb-network/skills/`), NOT inside `.claude-plugin/`.

Create all necessary parent directories before writing files.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.claude-plugin/marketplace.json','utf8')); console.log('marketplace.json valid')" && node -e "JSON.parse(require('fs').readFileSync('plugins/agentbnb-network/.claude-plugin/plugin.json','utf8')); console.log('plugin.json valid')" && test -f plugins/agentbnb-network/skills/agentbnb-network/SKILL.md && echo "SKILL.md exists"</automated>
  </verify>
  <done>
    - .claude-plugin/marketplace.json exists with name "agentbnb", owner "Cheng Wen Chen", one plugin entry pointing to ./plugins/agentbnb-network, NO version in plugin entry
    - plugins/agentbnb-network/.claude-plugin/plugin.json exists with name "agentbnb-network", version "2.2.0", author, license
    - plugins/agentbnb-network/skills/agentbnb-network/SKILL.md exists with frontmatter (name, description, disable-model-invocation) and setup guide body
    - skills/ directory is at plugin root, NOT inside .claude-plugin/
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md frontmatter and bump package.json version</name>
  <files>skills/agentbnb/SKILL.md, package.json</files>
  <action>
**1. Update `skills/agentbnb/SKILL.md` frontmatter** to comply with AgentSkills open standard.

Replace the current frontmatter:
```yaml
---
name: agentbnb
version: 2.0.0
description: "..."
author: AgentBnB
requires:
  runtime: node >= 20
  package_manager: pnpm
entry_point: bootstrap.ts
install_script: install.sh
---
```

With AgentSkills-standard frontmatter:
```yaml
---
name: agentbnb
description: "P2P capability sharing for AI agents — earn credits by sharing idle APIs, spend credits to request capabilities from peer agents. Use when an agent needs to join the AgentBnB network, publish capability cards, manage credits, or request skills from peers."
license: MIT
compatibility: "Requires Node.js >= 20 and pnpm. Designed for OpenClaw agents. Compatible with Claude Code, Gemini CLI, and other AgentSkills-compatible tools."
metadata:
  author: "Cheng Wen Chen"
  version: "2.2.0"
  tags: "ai-agent-skill,claude-code,agent-skills,p2p,capability-sharing"
---
```

Keep the entire body content (everything below the closing `---`) unchanged. Only the frontmatter changes.

Key changes:
- `version` moved from top-level to `metadata.version` (not a standard field at top level)
- `author` moved from top-level to `metadata.author` with real name
- `requires` replaced with `compatibility` (AgentSkills spec field)
- `entry_point` and `install_script` removed from frontmatter (documented in body)
- Added `license`, `metadata.tags` per spec
- Version bumped from 2.0.0 to 2.2.0

**2. Bump `package.json` version** from "1.0.0" to "2.2.0" to match the milestone and plugin version. Change ONLY the `"version"` field — do not modify any other field.
  </action>
  <verify>
    <automated>node -e "const pkg=JSON.parse(require('fs').readFileSync('package.json','utf8')); if(pkg.version!=='2.2.0') throw new Error('version is '+pkg.version); console.log('package.json version OK')" && node -e "const fs=require('fs'); const content=fs.readFileSync('skills/agentbnb/SKILL.md','utf8'); if(!content.includes('metadata:')) throw new Error('missing metadata block'); if(!content.includes('tags:')) throw new Error('missing tags'); if(content.includes('entry_point:')) throw new Error('still has entry_point'); console.log('SKILL.md frontmatter OK')"</automated>
  </verify>
  <done>
    - skills/agentbnb/SKILL.md frontmatter uses AgentSkills standard: name, description, license, compatibility, metadata (author, version, tags)
    - No non-standard top-level fields (version, author, requires, entry_point, install_script removed from top level)
    - metadata.version is "2.2.0"
    - metadata.author is "Cheng Wen Chen"
    - Body content of SKILL.md is unchanged
    - package.json version is "2.2.0"
  </done>
</task>

</tasks>

<verification>
- All JSON files parse without errors
- marketplace.json references plugin at correct relative path
- plugin.json has version 2.2.0 with no version in marketplace entry
- Plugin SKILL.md has correct frontmatter for Claude Code skill system
- Existing SKILL.md uses AgentSkills-standard frontmatter with metadata map
- package.json version matches plugin version at 2.2.0
- Existing test suite still passes: `pnpm vitest run src/registry/server.test.ts`
</verification>

<success_criteria>
1. `node -e "JSON.parse(require('fs').readFileSync('.claude-plugin/marketplace.json','utf8'))"` exits 0
2. `node -e "JSON.parse(require('fs').readFileSync('plugins/agentbnb-network/.claude-plugin/plugin.json','utf8'))"` exits 0
3. Plugin SKILL.md exists at `plugins/agentbnb-network/skills/agentbnb-network/SKILL.md`
4. `skills/agentbnb/SKILL.md` contains `metadata:` and does NOT contain `entry_point:`
5. `package.json` version is `"2.2.0"`
</success_criteria>

<output>
After completion, create `.planning/phases/15-distribution-discovery/15-01-SUMMARY.md`
</output>
