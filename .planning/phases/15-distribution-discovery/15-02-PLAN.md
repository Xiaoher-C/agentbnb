---
phase: 15-distribution-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: false
requirements:
  - DIST-04
  - DIST-05

must_haves:
  truths:
    - "GitHub repository has the three required topics: ai-agent-skill, claude-code, agent-skills"
    - "README shows a hub screenshot image reference that renders in GitHub preview"
    - "README has one-line install commands for Claude Code, OpenClaw, Antigravity, and CLI"
    - "README has badge images for Claude Code plugin and Agent Skills compatibility"
  artifacts:
    - path: "README.md"
      provides: "Updated README with badges, install commands, and screenshot"
      contains: "Claude Code"
    - path: "docs/hub-screenshot.png"
      provides: "Hub screenshot for README rendering"
  key_links:
    - from: "README.md"
      to: "docs/hub-screenshot.png"
      via: "Markdown image reference"
      pattern: "docs/hub-screenshot.png"
    - from: "README.md"
      to: ".claude-plugin/marketplace.json"
      via: "Install commands reference the marketplace"
      pattern: "plugin marketplace add"
---

<objective>
Set GitHub repository topics for discoverability and overhaul README with badges, per-tool install commands, and a hub screenshot.

Purpose: Makes the repository discoverable via GitHub topic search and gives visitors a polished first impression with clear install paths for every supported tool.

Output: Updated README.md, GitHub topics set, hub screenshot captured
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-distribution-discovery/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set GitHub topics and overhaul README</name>
  <files>README.md</files>
  <action>
**Part A: Set GitHub repository topics (DIST-04)**

Run the following command to set the three required topics:
```bash
gh api -X PUT /repos/Xiaoher-C/agentbnb/topics \
  -f "names[]=ai-agent-skill" \
  -f "names[]=claude-code" \
  -f "names[]=agent-skills"
```

If authentication fails, note the failure in the summary and proceed with Part B. The user can authenticate later.

**Part B: Overhaul README.md (DIST-05)**

Rewrite README.md with the following structure. Preserve all existing content sections but reorganize and add new sections.

**Badges** — Add at the top, after the title line, alongside existing npm/node/MIT badges:
```markdown
[![Claude Code Plugin](https://img.shields.io/badge/Claude%20Code-Plugin-orange.svg)](https://github.com/Xiaoher-C/agentbnb)
[![Agent Skills](https://img.shields.io/badge/Agent%20Skills-Compatible-blue.svg)](https://agentskills.io)
```

**Install section** — Add a new `## Install` section immediately after "The Core Idea" section and before "Agent Hub". Use a table format with one-line commands for each tool:

```markdown
## Install

| Tool | Command |
|------|---------|
| **Claude Code** | Add marketplace: `/plugin marketplace add Xiaoher-C/agentbnb`<br>Install: `/plugin install agentbnb-network@agentbnb` |
| **OpenClaw** | `openclaw install agentbnb` |
| **Antigravity** | `antigravity install agentbnb` |
| **CLI (npm)** | `npm install -g agentbnb` |
| **CLI (pnpm)** | `pnpm add -g agentbnb` |
```

Note: Antigravity command is unverified (tool not yet in AgentSkills ecosystem list). Add an HTML comment near it: `<!-- Antigravity install command unverified -->`.

**Hub screenshot** — The existing README already references `docs/hub-screenshot.png`. Keep this reference. The screenshot will be captured in Task 2 (checkpoint). If the `docs/` directory does not exist, create it with `mkdir -p docs`.

**Keep all other sections** (Core Idea, Features, Multi-Skill Capability Card, Autonomy Tiers, Auto-Share + Auto-Request, Requirements, Quick Start, Two-Machine Setup, OpenClaw Integration, Commands Reference, Architecture, Development, Examples, License). Reorder OpenClaw Integration to be adjacent to the Install section for better flow: Install -> OpenClaw Integration -> Agent Hub -> Features -> ...

Ensure the OpenClaw Integration section now starts with a reference to the Install table above rather than duplicating install instructions.

Do NOT remove any existing content. Add the new sections and reorganize.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('README.md','utf8'); const checks=['Claude%20Code-Plugin','Agent%20Skills-Compatible','plugin marketplace add','openclaw install','antigravity install','npm install -g agentbnb','pnpm add -g agentbnb','hub-screenshot.png']; const missing=checks.filter(c=>!r.includes(c)); if(missing.length) throw new Error('README missing: '+missing.join(', ')); console.log('README content checks passed')"</automated>
  </verify>
  <done>
    - GitHub topics set to ai-agent-skill, claude-code, agent-skills (or auth failure noted)
    - README has Claude Code Plugin and Agent Skills badges
    - README has Install table with commands for Claude Code, OpenClaw, Antigravity, CLI (npm + pnpm)
    - README references docs/hub-screenshot.png
    - All existing README content preserved
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify README rendering and capture hub screenshot</name>
  <files>docs/hub-screenshot.png</files>
  <action>
Human verifies that the README renders correctly and provides the hub screenshot.

What was built:
- GitHub repository topics set (ai-agent-skill, claude-code, agent-skills)
- README.md overhauled with badges, per-tool install table, reorganized sections

What still needs human action:
- Capture hub screenshot: run `agentbnb serve`, open http://localhost:7700/hub, screenshot the Discover page, save to docs/hub-screenshot.png
- Or place an existing screenshot at docs/hub-screenshot.png

Verification steps:
1. Open README.md in GitHub preview or VS Code preview — verify badges render, install table is readable
2. Verify GitHub topics: visit https://github.com/Xiaoher-C/agentbnb and check topics are shown
3. Confirm hub screenshot exists at docs/hub-screenshot.png and renders in README preview
4. Check all install commands are present: Claude Code, OpenClaw, Antigravity, npm, pnpm
  </action>
  <verify>
    <automated>test -f docs/hub-screenshot.png && echo "Screenshot exists" || echo "MISSING: docs/hub-screenshot.png"</automated>
  </verify>
  <done>
    - README renders correctly in GitHub preview with badges and install table
    - Hub screenshot exists at docs/hub-screenshot.png
    - GitHub topics visible on repository page
    - Human approved the visual rendering
  </done>
</task>

</tasks>

<verification>
- GitHub topics confirmed via `gh api repos/Xiaoher-C/agentbnb/topics` returning the three required topics
- README contains all required badges, install commands, and screenshot reference
- Hub screenshot exists at docs/hub-screenshot.png
- Existing test suite not broken (this plan modifies no code)
</verification>

<success_criteria>
1. `gh api repos/Xiaoher-C/agentbnb/topics` returns names including "ai-agent-skill", "claude-code", "agent-skills"
2. README.md contains Claude Code Plugin badge and Agent Skills badge
3. README.md contains install commands for all 4 tools (Claude Code, OpenClaw, Antigravity, CLI)
4. docs/hub-screenshot.png exists and README image reference points to it
5. Human has approved the README visual rendering
</success_criteria>

<output>
After completion, create `.planning/phases/15-distribution-discovery/15-02-SUMMARY.md`
</output>
