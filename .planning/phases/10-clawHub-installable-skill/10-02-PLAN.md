---
phase: 10-clawHub-installable-skill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/agentbnb/install.sh
  - skills/agentbnb/HEARTBEAT.rules.md
autonomous: true
requirements: [CLW-02, CLW-04]

must_haves:
  truths:
    - "Running install.sh after cloning installs the agentbnb CLI, initializes config, and syncs capabilities from SOUL.md"
    - "install.sh works without any human intervention — a new agent can join the network without reading documentation"
    - "HEARTBEAT.rules.md contains a standalone autonomy rules block that any agent can copy-paste directly into its HEARTBEAT.md"
    - "HEARTBEAT.rules.md uses the same marker format as heartbeat-writer.ts so injectHeartbeatSection() can merge it"
  artifacts:
    - path: "skills/agentbnb/install.sh"
      provides: "Post-install automation script for OpenClaw agents"
      contains: "agentbnb"
    - path: "skills/agentbnb/HEARTBEAT.rules.md"
      provides: "Standalone autonomy rules block for HEARTBEAT.md"
      contains: "agentbnb:start"
  key_links:
    - from: "skills/agentbnb/install.sh"
      to: "src/cli/index.ts"
      via: "invokes agentbnb CLI commands"
      pattern: "agentbnb"
    - from: "skills/agentbnb/HEARTBEAT.rules.md"
      to: "src/openclaw/heartbeat-writer.ts"
      via: "uses same marker format"
      pattern: "agentbnb:start"
---

<objective>
Create the install.sh post-install automation script and the HEARTBEAT.rules.md standalone autonomy rules file. install.sh makes a fresh agent join the AgentBnB network with zero manual steps. HEARTBEAT.rules.md gives agents a ready-to-paste block for governing AgentBnB behavior.

Purpose: These two files complete the "one command install" story — install.sh handles the technical setup, HEARTBEAT.rules.md provides the governance contract.
Output: `skills/agentbnb/install.sh` (executable) and `skills/agentbnb/HEARTBEAT.rules.md`.
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/openclaw/heartbeat-writer.ts

<interfaces>
<!-- Key contracts the executor needs -->

From src/openclaw/heartbeat-writer.ts:
```typescript
export const HEARTBEAT_MARKER_START = '<!-- agentbnb:start -->';
export const HEARTBEAT_MARKER_END = '<!-- agentbnb:end -->';

export function generateHeartbeatSection(
  autonomy: AutonomyConfig,
  budget: BudgetConfig,
): string;

export function injectHeartbeatSection(heartbeatPath: string, section: string): void;
```

From src/autonomy/tiers.ts:
```typescript
export interface AutonomyConfig { tier1_max_credits: number; tier2_max_credits: number; }
export const DEFAULT_AUTONOMY_CONFIG: AutonomyConfig; // { tier1_max_credits: 0, tier2_max_credits: 0 }
```

From src/credit/budget.ts:
```typescript
export interface BudgetConfig { reserve_credits: number; }
export const DEFAULT_BUDGET_CONFIG: BudgetConfig; // { reserve_credits: 20 }
```

CLI commands available (from src/cli/index.ts):
- `agentbnb init` — creates ~/.agentbnb/ config directory
- `agentbnb openclaw sync` — reads SOUL.md, publishes multi-skill card
- `agentbnb config set tier1 <N>` — set Tier 1 threshold
- `agentbnb config set tier2 <N>` — set Tier 2 threshold
- `agentbnb config set reserve <N>` — set reserve credits
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create install.sh post-install automation</name>
  <files>skills/agentbnb/install.sh</files>
  <action>
Create `skills/agentbnb/install.sh` as a bash script with the following behavior:

1. **Shebang and safety:** `#!/usr/bin/env bash` with `set -euo pipefail`

2. **Header comment:** Explain this is the post-install script for the AgentBnB OpenClaw skill

3. **Step 1 — Check prerequisites:**
   - Check if `node` is available (>= 20). If not, print error and exit 1.
   - Check if `pnpm` is available. If not, attempt `npm install -g pnpm` (warn if fails).

4. **Step 2 — Install AgentBnB CLI:**
   - Run `pnpm install -g agentbnb` (or if in a workspace context, just ensure the package is available)
   - Fall back to `npm install -g agentbnb` if pnpm global install fails
   - Verify `agentbnb --version` works after install

5. **Step 3 — Initialize config:**
   - Run `agentbnb init --yes` to create `~/.agentbnb/` with default config
   - This is idempotent — running on existing config is safe

6. **Step 4 — Sync from SOUL.md:**
   - Check if `SOUL.md` exists in the current directory or parent directory
   - If found, run `agentbnb openclaw sync` to publish the capability card
   - If not found, print a warning: "No SOUL.md found. Run 'agentbnb openclaw sync' manually after creating your SOUL.md."

7. **Step 5 — Print success message:**
   - Print a summary of what was set up
   - Print next steps: "Run 'agentbnb serve' to start accepting requests"

Use color output (green checkmarks, yellow warnings) via tput or ANSI codes with a fallback for non-color terminals. Each step prints its status.

Make the script idempotent — running it twice should not break anything.
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb && bash -n skills/agentbnb/install.sh && echo "Syntax OK" && test -x skills/agentbnb/install.sh && echo "Executable OK" || echo "FAIL: not executable"</automated>
  </verify>
  <done>
    - install.sh exists, is executable (chmod +x), and passes bash -n syntax check
    - Script has 5 steps: check prereqs, install CLI, init config, sync SOUL.md, print summary
    - Script uses set -euo pipefail for safety
    - Script is idempotent (safe to run multiple times)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HEARTBEAT.rules.md standalone autonomy rules</name>
  <files>skills/agentbnb/HEARTBEAT.rules.md</files>
  <action>
Create `skills/agentbnb/HEARTBEAT.rules.md` as a standalone markdown file containing the autonomy rules block that agents can copy-paste into their HEARTBEAT.md.

The content MUST use the same marker format as `heartbeat-writer.ts` for compatibility with `injectHeartbeatSection()`:

```
<!-- agentbnb:start -->
## AgentBnB Protocol

You are a participant in the AgentBnB capability sharing network.

### Sharing Rules
- When your idle_rate exceeds 70%, automatically share idle capabilities
- Accept incoming requests that match your published Capability Card
- Track credit earnings and report to owner weekly

### Requesting Rules
- When you encounter a task you cannot complete with local skills:
  1. Query AgentBnB network for matching capabilities
  2. If found and credit sufficient, automatically request
  3. Integrate result into your current workflow
- Budget limits:
  - < 10 credits: auto-execute, no notification (Tier 1)
  - 10-50 credits: execute, notify owner after (Tier 2)
  - > 50 credits: ask owner before executing (Tier 3)

### Credit Management
- Maintain minimum balance of 20 credits (reserve for emergencies)
- If balance drops below 20, increase sharing priority
- If balance exceeds 500, notify owner of surplus

### Autonomy Configuration
- Default: Tier 3 (ask before all actions) until owner configures thresholds
- Configure via: `agentbnb config set tier1 <credits>` and `agentbnb config set tier2 <credits>`
- Reserve floor: `agentbnb config set reserve <credits>` (default 20)
<!-- agentbnb:end -->
```

Use representative default values (Tier 1: 10, Tier 2: 50, reserve: 20) as examples since this is a template. Add a brief header above the markers explaining:
- This file contains the autonomy rules block for AgentBnB
- Copy the content between the markers into your HEARTBEAT.md
- Or run `agentbnb openclaw rules` to generate rules with your actual configured thresholds

NOTE: The values in this file are example defaults. The `agentbnb openclaw rules` command generates rules using the owner's actual configured thresholds. This file serves as a reference template.
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb && grep -c "agentbnb:start" skills/agentbnb/HEARTBEAT.rules.md && grep -c "agentbnb:end" skills/agentbnb/HEARTBEAT.rules.md && echo "Markers present"</automated>
  </verify>
  <done>
    - HEARTBEAT.rules.md exists with agentbnb:start and agentbnb:end markers
    - Contains sharing rules, requesting rules (3 tiers), and credit management section
    - Includes autonomy configuration section with CLI commands
    - Has explanatory header above the markers
    - Compatible with injectHeartbeatSection() from heartbeat-writer.ts
  </done>
</task>

</tasks>

<verification>
- install.sh passes `bash -n` syntax check and is executable
- HEARTBEAT.rules.md contains both `<!-- agentbnb:start -->` and `<!-- agentbnb:end -->` markers
- HEARTBEAT.rules.md content between markers is valid markdown
- install.sh steps match the success criteria: prereqs check, CLI install, config init, SOUL.md sync
</verification>

<success_criteria>
- A fresh agent can run `bash skills/agentbnb/install.sh` and be ready to serve on the network
- An agent can copy the rules block from HEARTBEAT.rules.md into its HEARTBEAT.md without modification
- Both files are self-documenting — no external documentation needed to understand their purpose
</success_criteria>

<output>
After completion, create `.planning/phases/10-clawHub-installable-skill/10-02-SUMMARY.md`
</output>
