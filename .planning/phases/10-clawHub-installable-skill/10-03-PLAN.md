---
phase: 10-clawHub-installable-skill
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - skills/agentbnb/SKILL.md
  - skills/agentbnb/bootstrap.test.ts
autonomous: true
requirements: [CLW-03, CLW-05]

must_haves:
  truths:
    - "SKILL.md contains agent-executable instructions with frontmatter metadata, on-install steps, autonomy rules, and CLI reference"
    - "An agent reading SKILL.md knows exactly what to do without human interpretation"
    - "The integration test confirms: mock SOUL.md, activate(), card published, gateway listening, IdleMonitor running, deactivate(), all resources cleaned up"
    - "After deactivate(), no cron jobs are running, no DB handles are open, no HTTP server is listening"
  artifacts:
    - path: "skills/agentbnb/SKILL.md"
      provides: "Agent-executable skill instructions with frontmatter"
      contains: "activate"
    - path: "skills/agentbnb/bootstrap.test.ts"
      provides: "Integration test for activate()/deactivate() lifecycle"
      contains: "activate"
  key_links:
    - from: "skills/agentbnb/bootstrap.test.ts"
      to: "skills/agentbnb/bootstrap.ts"
      via: "imports activate and deactivate"
      pattern: "import.*activate.*deactivate.*bootstrap"
    - from: "skills/agentbnb/SKILL.md"
      to: "skills/agentbnb/bootstrap.ts"
      via: "references activate() as the primary entry point"
      pattern: "activate"
    - from: "skills/agentbnb/SKILL.md"
      to: "skills/agentbnb/install.sh"
      via: "references install.sh as the setup script"
      pattern: "install.sh"
---

<objective>
Rewrite SKILL.md as agent-executable instructions and create the integration test that proves the full activate()/deactivate() lifecycle works end-to-end. SKILL.md must be readable and actionable by an agent (not a human developer). The integration test must confirm every resource is created on activate() and cleaned up on deactivate().

Purpose: SKILL.md is the "README for agents" — it replaces human documentation with machine-actionable instructions. The integration test is the proof that the bootstrap actually works.
Output: Rewritten `skills/agentbnb/SKILL.md` and `skills/agentbnb/bootstrap.test.ts`.
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-clawHub-installable-skill/10-01-SUMMARY.md
@.planning/phases/10-clawHub-installable-skill/10-02-SUMMARY.md

@skills/agentbnb/bootstrap.ts
@skills/agentbnb/install.sh
@skills/agentbnb/HEARTBEAT.rules.md

<interfaces>
<!-- Contracts from Plan 10-01 (bootstrap.ts) -->

From skills/agentbnb/bootstrap.ts (created in 10-01):
```typescript
export interface BootstrapConfig {
  owner: string;
  soulMdPath: string;
  registryDbPath?: string;
  creditDbPath?: string;
  gatewayPort?: number;
  gatewayToken?: string;
  handlerUrl?: string;
  autonomyConfig?: AutonomyConfig;
  silent?: boolean;
}

export interface BootstrapContext {
  runtime: AgentRuntime;
  gateway: FastifyInstance;
  idleMonitor: IdleMonitor;
  card: CapabilityCardV2;
}

export function activate(config: BootstrapConfig): Promise<BootstrapContext>;
export function deactivate(ctx: BootstrapContext): Promise<void>;
```

From src/runtime/agent-runtime.ts:
```typescript
export class AgentRuntime {
  readonly registryDb: Database.Database;
  readonly creditDb: Database.Database;
  readonly jobs: Cron[];
  get isDraining(): boolean;
  async shutdown(): Promise<void>;
}
```

Test pattern from src/runtime/agent-runtime.test.ts:
- Uses vitest (describe, it, expect, beforeEach, afterEach)
- Uses ':memory:' for SQLite paths in tests
- Cleans up with runtime.shutdown() in afterEach
- Tests both positive behavior and edge cases
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md as agent-executable instructions</name>
  <files>skills/agentbnb/SKILL.md</files>
  <action>
Rewrite `skills/agentbnb/SKILL.md` completely. The current version is human-oriented documentation. The new version must be agent-executable instructions — structured so an agent can read it and know exactly what to do.

Structure:

1. **Frontmatter** (YAML):
```yaml
---
name: agentbnb
version: 2.0.0
description: "P2P capability sharing — earn credits by sharing idle APIs, spend credits to request capabilities from peers"
author: AgentBnB
requires:
  runtime: node >= 20
  package_manager: pnpm
entry_point: bootstrap.ts
install_script: install.sh
---
```

2. **One-line summary:** "This skill connects your agent to the AgentBnB P2P capability sharing network."

3. **## Quick Start** (the primary agent-executable section):
   - Step 1: Run `bash install.sh` (auto-installs CLI, inits config, syncs SOUL.md)
   - Step 2: Import and call `activate()` from `bootstrap.ts` with config
   - Step 3: When done, call `deactivate()` to clean up
   - Include the TypeScript code snippet for activate/deactivate

4. **## On Install** (what install.sh does):
   - Checks Node >= 20 and pnpm availability
   - Runs `pnpm install -g agentbnb`
   - Runs `agentbnb init --yes`
   - Runs `agentbnb openclaw sync` if SOUL.md exists

5. **## Programmatic API** (for agents that want fine-grained control):
   - `activate(config)` — returns BootstrapContext with runtime, gateway, idleMonitor, card
   - `deactivate(ctx)` — tears down all resources
   - Document BootstrapConfig fields with types and defaults
   - Document BootstrapContext fields

6. **## Autonomy Rules** (copy-paste governance):
   - Reference HEARTBEAT.rules.md for the full rules block
   - Summarize: 3 tiers (auto/notify/ask), reserve floor, idle sharing
   - Note: run `agentbnb openclaw rules` for rules with actual configured thresholds

7. **## CLI Reference** (commands the agent can invoke):
   - `agentbnb serve` — start accepting requests
   - `agentbnb openclaw sync` — sync SOUL.md to capability card
   - `agentbnb openclaw status` — check status, balance, idle rates
   - `agentbnb openclaw rules` — emit HEARTBEAT.md rules block
   - `agentbnb config set tier1|tier2|reserve <N>` — configure autonomy
   - `agentbnb discover` — find peers on the network
   - `agentbnb request --query "..."` — manually request a capability

8. **## Adapters** (for agents that need individual modules):
   - gateway.ts — AgentRuntime + createGatewayServer
   - auto-share.ts — IdleMonitor
   - auto-request.ts — AutoRequestor
   - credit-mgr.ts — BudgetManager + getBalance
   - Note: bootstrap.ts wires all of these; use adapters only if you need custom wiring

Key writing principles per user decision "SKILL.md must be agent-executable instructions, not human documentation":
- Use imperative instructions: "Run this", "Call this function", "Pass these parameters"
- Include exact code snippets an agent can execute
- No conversational prose or explanations of concepts
- Every section answers "what should I do?" not "what is this?"
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb && head -5 skills/agentbnb/SKILL.md | grep "name: agentbnb" && grep -c "activate" skills/agentbnb/SKILL.md && grep -c "install.sh" skills/agentbnb/SKILL.md && echo "SKILL.md structure OK"</automated>
  </verify>
  <done>
    - SKILL.md has YAML frontmatter with name, version, description, author, requires, entry_point, install_script
    - Contains Quick Start, On Install, Programmatic API, Autonomy Rules, CLI Reference, Adapters sections
    - All sections use imperative, agent-executable language
    - References bootstrap.ts activate()/deactivate() as primary entry point
    - References install.sh as the setup script
    - References HEARTBEAT.rules.md for autonomy governance
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create integration test for activate()/deactivate() lifecycle</name>
  <files>skills/agentbnb/bootstrap.test.ts</files>
  <behavior>
    - Test 1: activate() with mock SOUL.md returns BootstrapContext with valid runtime, gateway, idleMonitor, card
    - Test 2: After activate(), card is published (runtime.registryDb has the card)
    - Test 3: After activate(), gateway is listening (GET /health returns 200)
    - Test 4: After activate(), IdleMonitor cron job is registered in runtime.jobs
    - Test 5: After deactivate(), runtime.isDraining is true
    - Test 6: After deactivate(), DB handles are closed (queries throw)
    - Test 7: deactivate() is idempotent (calling twice does not throw)
    - Test 8: activate() throws when SOUL.md path does not exist
  </behavior>
  <action>
Create `skills/agentbnb/bootstrap.test.ts` as a vitest integration test.

**Test setup:**
- Create a temporary SOUL.md file in a temp directory (using `node:fs` mkdtempSync + writeFileSync)
- Use `:memory:` for both registryDbPath and creditDbPath
- Use `silent: true` on BootstrapConfig to suppress gateway logs
- Use port 0 or a random high port (e.g., 17700 + random offset) to avoid conflicts. NOTE: Fastify with port 0 auto-assigns a free port.
- Clean up temp files in afterEach

**Mock SOUL.md content:**
```markdown
# Test Agent

A test agent for integration testing.

## Code Review
Reviews code for quality and bugs.

## Translation
Translates text between languages.
```

**Test cases:**

1. **"activate() returns BootstrapContext with all components"**
   - Call activate() with config
   - Assert ctx.runtime is an instance with registryDb and creditDb
   - Assert ctx.gateway is a Fastify instance
   - Assert ctx.idleMonitor is an IdleMonitor instance
   - Assert ctx.card has spec_version '2.0' and skills.length === 2
   - Call deactivate(ctx) in cleanup

2. **"activate() publishes card from SOUL.md into registry"**
   - After activate(), query runtime.registryDb for capability_cards
   - Assert exactly 1 card exists with owner matching config.owner
   - Assert the card's skills array has entries for "Code Review" and "Translation"

3. **"activate() starts gateway that responds to health check"**
   - After activate(), make a GET request to the gateway's /health endpoint
   - Use ctx.gateway.inject({ method: 'GET', url: '/health' }) (Fastify inject, no real HTTP needed)
   - Assert status 200 and body contains { status: 'ok' }

4. **"activate() registers IdleMonitor job in runtime.jobs"**
   - After activate(), check runtime.jobs.length >= 1
   - The registered job should be the IdleMonitor's cron job

5. **"deactivate() sets runtime.isDraining to true"**
   - Activate, then deactivate
   - Assert ctx.runtime.isDraining === true

6. **"deactivate() closes DB handles"**
   - Activate, then deactivate
   - Assert ctx.runtime.registryDb.prepare('SELECT 1').get() throws

7. **"deactivate() is idempotent"**
   - Activate, deactivate, deactivate again
   - Second call should not throw

8. **"activate() throws when SOUL.md does not exist"**
   - Call activate() with a non-existent soulMdPath
   - Assert it rejects with an error (AgentBnBError or similar)

Use `describe('bootstrap activate/deactivate lifecycle', () => { ... })` wrapper.
Use afterEach to ensure deactivate() is called even if a test fails (track ctx in a variable).

**Important:** Use `await ctx.gateway.ready()` after activate if needed before calling inject(). The activate() function should handle this internally (gateway.listen() implies ready), but verify.

Import paths: `import { activate, deactivate } from './bootstrap.js'` (co-located test).
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb && npx vitest run skills/agentbnb/bootstrap.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - bootstrap.test.ts exists with 8 test cases covering the full lifecycle
    - All 8 tests pass: activate returns context, card published, gateway listening, idle job registered, deactivate cleans up, idempotent deactivate, missing SOUL.md throws
    - Test uses in-memory DBs and temp SOUL.md — no side effects on real filesystem
    - Test cleans up all resources in afterEach
  </done>
</task>

</tasks>

<verification>
- SKILL.md has YAML frontmatter and agent-executable sections
- `npx vitest run skills/agentbnb/bootstrap.test.ts` passes all 8 tests
- No orphaned DB handles or temp files after tests complete
</verification>

<success_criteria>
- An agent reading SKILL.md can go from zero to running on the AgentBnB network using only the instructions in the file
- The integration test proves the full lifecycle: create -> publish -> serve -> monitor -> teardown with zero resource leaks
- All 5 requirement IDs (CLW-01 through CLW-05) are verified by the combined plans
</success_criteria>

<output>
After completion, create `.planning/phases/10-clawHub-installable-skill/10-03-SUMMARY.md`
</output>
