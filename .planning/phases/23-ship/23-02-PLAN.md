---
phase: 23-ship
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - .github/FUNDING.yml
  - docs/social-preview.html
autonomous: true
requirements:
  - SHIP-01
  - SHIP-03

must_haves:
  truths:
    - "No API keys, tokens, or secrets exist in git history"
    - "GitHub repository metadata is set for discoverability (topics, social preview)"
    - "My Agent route already works at /#/myagent (pre-existing, verified)"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Claude Code marketplace listing"
      min_lines: 1
    - path: "LICENSE"
      provides: "MIT license with correct copyright"
      contains: "2026 Cheng Wen Chen"
    - path: ".gitignore"
      provides: "Excludes secrets and build artifacts"
      contains: ".env"
  key_links:
    - from: ".gitignore"
      to: ".env"
      via: "Prevents secret files from being committed"
      pattern: "\\.env"
---

<objective>
Run GitHub public readiness checklist: secrets scan, verify existing files (LICENSE, .gitignore, marketplace.json, AGENT-NATIVE-PROTOCOL.md), generate social preview image, and confirm My Agent route works.

Purpose: Ensure the repo is safe and polished for public visibility.
Output: Verified checklist, social preview image ready for GitHub settings.
</objective>

<execution_context>
@/Users/leyufounder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leyufounder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ship/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secrets scan + public readiness verification</name>
  <files></files>
  <action>
Run a comprehensive secrets scan across the entire git history and working tree. This is a READ-ONLY verification task — no files are created or modified.

Step 1 — Scan git history for secrets:
```bash
git log --all -p 2>/dev/null | grep -iE "(api_key|secret_key|private_key|token|password|bearer)" | grep -vE "(\.env\.example|CLAUDE\.md|CONTEXT\.md|milestone|REQUIREMENTS|PLAN|SKILL\.md|\.test\.|mock|fixture|README)" | head -50
```
If any real secrets found, report them as a blocker. False positives from test fixtures, documentation, and env examples are expected and safe.

Step 2 — Verify critical files exist and are correct:
- `LICENSE` contains "MIT" and "2026 Cheng Wen Chen"
- `.gitignore` contains `.env`, `*.db`, `node_modules/`
- `README.md` exists and has content
- `AGENT-NATIVE-PROTOCOL.md` exists in repo root
- `.claude-plugin/marketplace.json` exists
- `docs/brain/` directory exists with files

Step 3 — Verify My Agent route (SHIP-01 is pre-existing):
```bash
grep -r "myagent\|my-agent\|MyAgent\|OwnerDashboard" hub/src/ --include="*.tsx" --include="*.ts" | head -10
```
Confirm the route exists in Hub's router. This is verification only — SHIP-01 was completed in a prior phase.

Step 4 — Check for large files that should not be in a public repo:
```bash
find . -not -path "./.git/*" -not -path "*/node_modules/*" -size +5M 2>/dev/null
```

Report results as a verification summary. If secrets are found, they must be cleaned before going public.
  </action>
  <verify>
    <automated>test -f LICENSE && test -f AGENT-NATIVE-PROTOCOL.md && test -f .claude-plugin/marketplace.json && test -d docs/brain && echo "All critical files present"</automated>
  </verify>
  <done>Secrets scan finds no real secrets in git history. All required files verified present: LICENSE, .gitignore, README.md, AGENT-NATIVE-PROTOCOL.md, marketplace.json, docs/brain/. My Agent route confirmed in Hub router. No large binary files found.</done>
</task>

<task type="auto">
  <name>Task 2: Social preview HTML + GitHub metadata prep</name>
  <files>docs/social-preview.html</files>
  <action>
Create `docs/social-preview.html` — a standalone HTML file that renders a 1280x640 social preview card (GitHub's recommended Open Graph dimensions). This will be screenshot-captured and uploaded as the repo's social preview image.

Design (matching Hub's dark theme):
- Background: #08080C (hub dark bg)
- Title: "AgentBnB" in white, large (64px), Inter font
- Subtitle: "P2P Agent Capability Sharing Protocol" in #9CA3AF (gray-400)
- Tagline: "Airbnb for AI Agent Pipelines" in #10B981 (emerald accent)
- Include the doodle creature SVG (copy from hub/src/components/layout/NavBar.tsx)
- Bottom: "agentbnb.dev" in small text, MIT badge

The file should be self-contained HTML+CSS (no external deps) so it can be opened in any browser and screenshotted at 1280x640.

Also create a brief note in the SUMMARY about GitHub settings that need manual configuration:
- Repository topics: `ai-agent`, `p2p`, `capability-sharing`, `typescript`, `agent-protocol`, `claude-code`
- Social preview: upload screenshot of docs/social-preview.html
- These are GitHub UI settings — Claude cannot set them via CLI without `gh` repo edit (topics can be set via `gh repo edit --add-topic`)
  </action>
  <verify>
    <automated>test -f docs/social-preview.html && grep -q "AgentBnB" docs/social-preview.html && grep -q "1280" docs/social-preview.html && echo "Social preview HTML exists with correct dimensions"</automated>
  </verify>
  <done>Social preview HTML renders at 1280x640 with dark theme, AgentBnB branding, doodle creature, and tagline. File is self-contained and ready for screenshot.</done>
</task>

</tasks>

<verification>
- Secrets scan produces no real secret matches in git history
- All critical files verified present (LICENSE, .gitignore, README, AGENT-NATIVE-PROTOCOL.md, marketplace.json)
- My Agent route exists in Hub router
- Social preview HTML renders correctly at 1280x640
</verification>

<success_criteria>
Repository passes all public readiness checks. No secrets in history. Social preview image ready. All metadata files in place. SHIP-01 confirmed working (pre-existing). SHIP-03 checklist complete.
</success_criteria>

<output>
After completion, create `.planning/phases/23-ship/23-02-SUMMARY.md`
</output>
