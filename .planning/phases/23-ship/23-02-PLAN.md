---
phase: 23-ship
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - docs/social-preview.html
  - hub/src/pages/SkillExecutorPage.tsx
  - hub/src/main.tsx
autonomous: true
requirements:
  - SHIP-01
  - SHIP-03

must_haves:
  truths:
    - "No API keys, tokens, or secrets exist in git history"
    - "GitHub repository metadata is set for discoverability (topics, social preview)"
    - "My Agent route already works at /#/myagent (pre-existing, verified)"
    - "Hub has basic v3.0 feature awareness (at minimum a status/info section)"
  artifacts:
    - path: "docs/social-preview.html"
      provides: "Social preview card for GitHub"
      contains: "AgentBnB"
    - path: "LICENSE"
      provides: "MIT license with correct copyright"
      contains: "2026 Cheng Wen Chen"
    - path: ".gitignore"
      provides: "Excludes secrets and build artifacts"
      contains: ".env"
  key_links:
    - from: ".gitignore"
      to: ".env"
      via: "Prevents secret files from being committed"
      pattern: "\\.env"
---

<objective>
Run GitHub public readiness checklist (secrets scan, file verification, social preview) AND add basic Hub v3.0 awareness.

Purpose: Ensure repo is safe and polished for public visibility. Hub shows v3.0 capabilities exist.
</objective>

<execution_context>
@/Users/leyufounder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leyufounder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ship/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secrets scan + public readiness verification</name>
  <files></files>
  <action>
Run a comprehensive secrets scan across the entire git history and working tree. This is a READ-ONLY verification task — no files are created or modified.

Step 1 — Scan git history for secrets:
```bash
git log --all -p 2>/dev/null | grep -iE "(api_key|secret_key|private_key|token|password|bearer)" | grep -vE "(\.env\.example|CLAUDE\.md|CONTEXT\.md|milestone|REQUIREMENTS|PLAN|SKILL\.md|\.test\.|mock|fixture|README|SUMMARY|config\.json)" | head -50
```
If any real secrets found, report them as a blocker. False positives from test fixtures, documentation, and env examples are expected and safe.

Step 2 — Verify critical files exist and are correct:
- `LICENSE` contains "MIT" and "2026 Cheng Wen Chen"
- `.gitignore` contains `.env`, `*.db`, `node_modules/`
- `README.md` exists and has content
- `AGENT-NATIVE-PROTOCOL.md` exists in repo root
- `.claude-plugin/marketplace.json` exists
- `docs/brain/` directory exists with files

Step 3 — Verify My Agent route (SHIP-01 is pre-existing):
```bash
grep -r "myagent\|my-agent\|MyAgent\|OwnerDashboard" hub/src/ --include="*.tsx" --include="*.ts" | head -10
```
Confirm the route exists in Hub's router.

Step 4 — Check for large files:
```bash
find . -not -path "./.git/*" -not -path "*/node_modules/*" -size +5M 2>/dev/null
```

Report results as a verification summary.
  </action>
  <verify>
    <automated>test -f LICENSE && test -f AGENT-NATIVE-PROTOCOL.md && test -d docs/brain && echo "All critical files present"</automated>
  </verify>
  <done>Secrets scan finds no real secrets. All required files verified. My Agent route confirmed. No large binary files found.</done>
</task>

<task type="auto">
  <name>Task 2: Social preview + Hub v3.0 awareness + GitHub metadata</name>
  <files>docs/social-preview.html, hub/src/pages/SkillExecutorPage.tsx, hub/src/main.tsx</files>
  <action>
Step 1 — Create `docs/social-preview.html`:
A standalone HTML file (1280x640) for GitHub social preview. Design:
- Background: #08080C (hub dark bg)
- Title: "AgentBnB" in white, large (64px), Inter font
- Subtitle: "P2P Agent Capability Sharing Protocol" in #9CA3AF
- Tagline: "Airbnb for AI Agent Pipelines" in #10B981 (emerald)
- Include doodle creature SVG from hub NavBar
- Bottom: "agentbnb.dev" + MIT badge
Self-contained HTML+CSS, no external deps.

Step 2 — Add basic Hub v3.0 feature awareness:

The Hub currently has zero awareness of v3.0 features. Add a lightweight "v3.0 Features" info section to the Docs page (hub/src/pages/DocsPage.tsx) or as a new section within the existing structure:

- **SkillExecutor**: Brief description that agents can now execute skills via config-driven `skills.yaml`
- **Conductor**: Brief description of multi-agent task orchestration
- **Signed Escrow**: Brief description of cross-machine credit verification

This should be a documentation/info section (not a functional UI), fitting the existing dark theme. Read the existing DocsPage.tsx first to match its pattern.

Step 3 — Note GitHub metadata for manual setup:
- Topics: `ai-agent`, `p2p`, `capability-sharing`, `typescript`, `agent-protocol`, `claude-code`
- Social preview: upload screenshot of docs/social-preview.html
- These require GitHub UI or `gh repo edit --add-topic`
  </action>
  <verify>
    <automated>test -f docs/social-preview.html && grep -q "AgentBnB" docs/social-preview.html && grep -q "1280" docs/social-preview.html && echo "Social preview exists"</automated>
  </verify>
  <done>Social preview HTML renders at 1280x640. Hub Docs page includes v3.0 feature descriptions. GitHub metadata steps documented.</done>
</task>

</tasks>

<verification>
- Secrets scan produces no real secret matches
- All critical files present (LICENSE, .gitignore, README, AGENT-NATIVE-PROTOCOL.md, marketplace.json)
- Social preview HTML renders correctly at 1280x640
- Hub has v3.0 feature information visible
- My Agent route confirmed working
</verification>

<success_criteria>
Repository passes all public readiness checks. Social preview ready. Hub acknowledges v3.0 features. SHIP-01 confirmed (pre-existing). SHIP-03 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/23-ship/23-02-SUMMARY.md`
</output>
</content>
