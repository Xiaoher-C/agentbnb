---
phase: 17-below-fold-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/components/CompatibleWithSection.tsx
  - hub/src/components/CompatibleWithSection.test.tsx
  - hub/src/components/FAQSection.tsx
  - hub/src/components/FAQSection.test.tsx
  - hub/src/components/ValuePropSection.tsx
  - hub/src/components/ValuePropSection.test.tsx
  - hub/src/pages/DiscoverPage.tsx
autonomous: true
requirements:
  - FOLD-01
  - FOLD-02
  - FOLD-03
  - FOLD-04

must_haves:
  truths:
    - "A 'Compatible With' section with scrolling marquee of tool names appears below the Discover card grid"
    - "A FAQ accordion with 5-6 questions about AgentBnB is visible below Compatible With"
    - "A brief value proposition paragraph explains the protocol below the FAQ"
    - "All below-fold sections use hub-* design tokens and feel native to the dark Hub aesthetic"
  artifacts:
    - path: "hub/src/components/CompatibleWithSection.tsx"
      provides: "Marquee-based tool compatibility strip"
      exports: ["CompatibleWithSection"]
    - path: "hub/src/components/FAQSection.tsx"
      provides: "Accordion-based FAQ section"
      exports: ["FAQSection"]
    - path: "hub/src/components/ValuePropSection.tsx"
      provides: "Value proposition text section"
      exports: ["ValuePropSection"]
    - path: "hub/src/pages/DiscoverPage.tsx"
      provides: "Discover route with below-fold sections appended"
  key_links:
    - from: "hub/src/pages/DiscoverPage.tsx"
      to: "hub/src/components/CompatibleWithSection.tsx"
      via: "import and render after card grid"
      pattern: "import.*CompatibleWithSection"
    - from: "hub/src/pages/DiscoverPage.tsx"
      to: "hub/src/components/FAQSection.tsx"
      via: "import and render after CompatibleWithSection"
      pattern: "import.*FAQSection"
    - from: "hub/src/pages/DiscoverPage.tsx"
      to: "hub/src/components/ValuePropSection.tsx"
      via: "import and render after FAQSection"
      pattern: "import.*ValuePropSection"
    - from: "hub/src/components/CompatibleWithSection.tsx"
      to: "hub/src/components/ui/marquee.tsx"
      via: "Marquee component import"
      pattern: "import.*Marquee.*from.*ui/marquee"
    - from: "hub/src/components/FAQSection.tsx"
      to: "hub/src/components/ui/accordion.tsx"
      via: "Accordion component imports"
      pattern: "import.*Accordion.*from.*ui/accordion"
---

<objective>
Add three below-fold content sections to the Discover page: a "Compatible With" marquee, a FAQ accordion, and a value proposition paragraph.

Purpose: Give first-time visitors supporting context below the capability card grid — what tools work with AgentBnB, common questions answered, and a concise explanation of the protocol.
Output: Three new section components wired into DiscoverPage, all tests passing.
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-below-fold-sections/17-RESEARCH.md

<interfaces>
<!-- Existing components the executor will use directly -->

From hub/src/components/ui/marquee.tsx:
```typescript
interface MarqueeProps extends ComponentPropsWithoutRef<'div'> {
  className?: string;
  reverse?: boolean;
  pauseOnHover?: boolean;
  children: React.ReactNode;
  vertical?: boolean;
  repeat?: number;  // default 4
}
export function Marquee(props: MarqueeProps): JSX.Element;
```

From hub/src/components/ui/accordion.tsx:
```typescript
// All four re-export Radix primitives with Hub dark theme styling
export function Accordion(props: AccordionPrimitive.RootProps): JSX.Element;
export function AccordionItem(props: AccordionPrimitive.ItemProps): JSX.Element;
export function AccordionTrigger(props: AccordionPrimitive.TriggerProps): JSX.Element;
export function AccordionContent(props: AccordionPrimitive.ContentProps): JSX.Element;
// IMPORTANT: Accordion requires type="single" collapsible OR type="multiple"
```

From hub/src/pages/DiscoverPage.tsx:
```typescript
// Current structure — sections append after the card grid conditional block
export default function DiscoverPage(): JSX.Element;
// Uses useOutletContext<AppOutletContext>() for setSelectedCard
// Renders: StatsBar, SearchFilter, then loading/error/empty/CardGrid conditional
```

Design tokens (from hub/tailwind.config.js):
```
hub-bg:             #08080C
hub-surface:        rgba(255,255,255,0.03)
hub-border:         rgba(255,255,255,0.06)
hub-accent:         #10B981
hub-text-primary:   rgba(255,255,255,0.92)
hub-text-secondary: rgba(255,255,255,0.55)
hub-text-muted:     rgba(255,255,255,0.40)
```

Test pattern (from hub/src/components/CapabilityCard.test.tsx):
```typescript
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
// globals: true in vite.config.ts — describe/it/expect available globally
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create three below-fold section components with tests</name>
  <files>
    hub/src/components/CompatibleWithSection.tsx,
    hub/src/components/CompatibleWithSection.test.tsx,
    hub/src/components/FAQSection.tsx,
    hub/src/components/FAQSection.test.tsx,
    hub/src/components/ValuePropSection.tsx,
    hub/src/components/ValuePropSection.test.tsx
  </files>
  <behavior>
    - CompatibleWithSection renders a section with heading text "Compatible With"
    - CompatibleWithSection renders at least 10 tool names (Claude Code, OpenClaw, Antigravity, Cursor, Windsurf, Node.js, Python, TypeScript, JSON-RPC, HTTP)
    - FAQSection renders a section with heading text "FAQ"
    - FAQSection renders at least 5 accordion items with question text visible
    - FAQSection accordion items have answer content (testing AccordionContent text)
    - ValuePropSection renders a section with heading text "The Protocol"
    - ValuePropSection renders a paragraph containing "peer-to-peer" (core value prop keyword)
  </behavior>
  <action>
    Create test files FIRST (RED), then implement components (GREEN).

    **CompatibleWithSection.test.tsx:**
    - Test that "Compatible With" heading renders
    - Test that all 10 tool names appear in the document
    - Import and render CompatibleWithSection standalone (no router context needed)

    **CompatibleWithSection.tsx:**
    - Export named function `CompatibleWithSection`
    - Define TOOLS array: ['Claude Code', 'OpenClaw', 'Antigravity', 'Cursor', 'Windsurf', 'Node.js', 'Python', 'TypeScript', 'JSON-RPC', 'HTTP']
    - Section wrapper: `<section className="mt-16 border-t border-hub-border pt-12 pb-8">`
    - Heading: `<h2 className="text-center text-xs font-semibold uppercase tracking-widest text-hub-text-muted mb-6">Compatible With</h2>`
    - Use `<Marquee pauseOnHover className="[--duration:30s]">` wrapping ToolPill components
    - ToolPill: `<span className="mx-3 inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white/[0.04] border border-hub-border text-sm text-hub-text-secondary whitespace-nowrap">{name}</span>`
    - Import Marquee from '../components/ui/marquee.js' (relative from components dir, use './ui/marquee.js')

    **FAQSection.test.tsx:**
    - Test that "FAQ" heading renders
    - Test that all FAQ question texts appear
    - Test that answer content text exists in the DOM (AccordionContent renders hidden, so use `{ hidden: true }` option or check container.textContent)

    **FAQSection.tsx:**
    - Export named function `FAQSection`
    - Define FAQ_ITEMS array with 6 items:
      1. q: "What is AgentBnB?" / a: "AgentBnB is a peer-to-peer protocol where AI agents share idle capabilities with each other. Think Airbnb, but instead of spare rooms, agents list spare compute skills — and other agents book and use them autonomously."
      2. q: "How do credits work?" / a: "Each agent starts with 50 free credits. When your agent fulfills a request from another agent, you earn credits. When your agent uses someone else's capability, you spend credits. No real money involved — it's a lightweight exchange currency."
      3. q: "How do I list my agent's skills?" / a: "Run `agentbnb init` to auto-detect your agent's API keys and generate a Capability Card. Then `agentbnb publish` to make it discoverable. Your agent's idle capabilities are automatically shared based on your autonomy tier settings."
      4. q: "Which AI frameworks are supported?" / a: "AgentBnB works with any agent that speaks JSON-RPC over HTTP. It integrates with Claude Code, OpenClaw, Cursor, Windsurf, and any Node.js or Python agent runtime. The protocol is framework-agnostic by design."
      5. q: "Is it open source?" / a: "Yes. AgentBnB is MIT licensed and fully open source. The entire protocol, registry, Hub UI, and CLI are in the public repository. Contributions are welcome."
      6. q: "How do agents discover each other?" / a: "Agents discover peers through mDNS on local networks and through the public registry at agentbnb.dev. The registry supports full-text search across all published Capability Cards, so agents can find exactly the skills they need."
    - Section wrapper: `<section className="mt-12 border-t border-hub-border pt-12 pb-8">`
    - Heading: `<h2 className="text-xs font-semibold uppercase tracking-widest text-hub-text-muted mb-6">FAQ</h2>`
    - `<Accordion type="single" collapsible className="max-w-2xl">` wrapping AccordionItems
    - Each AccordionItem: value={id}, AccordionTrigger with question, AccordionContent with answer
    - Import Accordion components from './ui/accordion.js'

    **ValuePropSection.test.tsx:**
    - Test that "The Protocol" heading renders
    - Test that paragraph contains "peer-to-peer"

    **ValuePropSection.tsx:**
    - Export named function `ValuePropSection`
    - Section wrapper: `<section className="mt-12 border-t border-hub-border pt-12 pb-16">`
    - Heading: `<h2 className="text-xs font-semibold uppercase tracking-widest text-hub-text-muted mb-6">The Protocol</h2>`
    - `<div className="max-w-xl">` containing a `<p className="text-hub-text-secondary text-base leading-relaxed">` with text: "AgentBnB is a peer-to-peer protocol for AI agents to share idle capabilities and discover new ones. Agents list what they can do, set a credit price, and other agents book and use them — no human in the loop. The protocol runs on JSON-RPC over HTTP, works with any AI framework, and uses a lightweight credit system to keep exchanges fair."
    - Optionally add a second smaller paragraph: `<p className="mt-4 text-hub-text-tertiary text-sm">` with "Open source. MIT licensed. Built for agents, by agents."

    **FOLD-04 compliance (all components):**
    - Use ONLY hub-* token classes: text-hub-text-primary, text-hub-text-secondary, text-hub-text-muted, text-hub-text-tertiary, border-hub-border, bg-white/[0.04]
    - Do NOT use any hard-coded hex colors (#10B981, #08080C, etc.)
    - Do NOT set bg-hub-bg on section wrappers (page background shows through)
    - Do NOT import or use motion/react animations — keep sections static and native-feeling
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb/hub && pnpm vitest run src/components/CompatibleWithSection.test.tsx src/components/FAQSection.test.tsx src/components/ValuePropSection.test.tsx</automated>
  </verify>
  <done>
    - CompatibleWithSection renders "Compatible With" heading and 10 tool name pills inside a Marquee
    - FAQSection renders "FAQ" heading and 6 accordion items with questions and answers
    - ValuePropSection renders "The Protocol" heading and value prop paragraph
    - All three test files pass
    - No hard-coded hex colors — all use hub-* design tokens
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire below-fold sections into DiscoverPage</name>
  <files>hub/src/pages/DiscoverPage.tsx</files>
  <action>
    Modify DiscoverPage.tsx to import and render the three section components after the card grid conditional block.

    1. Add three imports at the top (after existing imports):
       ```
       import CompatibleWithSection from '../components/CompatibleWithSection.js';
       import FAQSection from '../components/FAQSection.js';
       import ValuePropSection from '../components/ValuePropSection.js';
       ```

    2. Inside the return JSX, append the three sections AFTER the loading/error/empty/CardGrid conditional block but BEFORE the closing `</>` fragment:
       ```
       <CompatibleWithSection />
       <FAQSection />
       <ValuePropSection />
       ```

    3. The sections render UNCONDITIONALLY — they show regardless of loading/error/empty state. They contain static content and do not depend on card data.

    4. Section order is: Cards -> Compatible With -> FAQ -> Value Prop (top to bottom). This puts the most visually engaging element (marquee) immediately after cards, informational FAQ in the middle, and the summary at the bottom.

    5. Do NOT modify the existing StatsBar, SearchFilter, or card grid conditional logic. Only add imports and the three component renders.
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb/hub && pnpm vitest run</automated>
  </verify>
  <done>
    - DiscoverPage imports and renders CompatibleWithSection, FAQSection, and ValuePropSection
    - Sections appear after the card grid in the JSX
    - All existing Hub tests still pass
    - Full Hub test suite green
  </done>
</task>

</tasks>

<verification>
1. All Hub tests pass: `cd hub && pnpm vitest run` — zero failures
2. Three new test files exercise section rendering and content
3. DiscoverPage.tsx imports all three sections and renders them after the card grid
4. No hard-coded hex colors in any new component (grep for `#[0-9a-fA-F]` returns 0 matches in new files)
5. All sections use hub-* design token classes
</verification>

<success_criteria>
- CompatibleWithSection, FAQSection, and ValuePropSection exist as standalone components
- All three have passing test files
- DiscoverPage renders all three sections below the card grid unconditionally
- Full Hub test suite passes (`cd hub && pnpm vitest run`)
- Visual: scrolling below the card grid reveals Compatible With marquee, FAQ accordion, and value prop text
- No visual regression — dark theme maintained, hub-* tokens used exclusively
</success_criteria>

<output>
After completion, create `.planning/phases/17-below-fold-sections/17-01-SUMMARY.md`
</output>
