---
phase: 00-dogfood
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/credit/ledger.ts
  - src/credit/escrow.ts
  - src/credit/ledger.test.ts
autonomous: true
requirements: [R-005]

must_haves:
  truths:
    - "New agents receive a bootstrap credit grant"
    - "Credits are deducted atomically during escrow hold"
    - "Insufficient balance prevents escrow hold"
    - "Successful execution settles escrow (credits go to capability owner)"
    - "Failed execution refunds escrowed credits"
    - "Transaction history is queryable and immutable"
  artifacts:
    - path: "src/credit/ledger.ts"
      provides: "Credit balance management and transaction log"
      exports: ["openCreditDb", "getBalance", "bootstrapAgent", "getTransactions"]
    - path: "src/credit/escrow.ts"
      provides: "Escrow hold, settle, and release operations"
      exports: ["holdEscrow", "settleEscrow", "releaseEscrow"]
    - path: "src/credit/ledger.test.ts"
      provides: "Full credit system test coverage"
  key_links:
    - from: "src/credit/escrow.ts"
      to: "src/credit/ledger.ts"
      via: "uses same database, updates balances and transactions atomically"
      pattern: "db\\.transaction"
    - from: "src/credit/ledger.ts"
      to: "src/types/index.ts"
      via: "uses AgentBnBError for insufficient credits"
      pattern: "AgentBnBError"
---

<objective>
Build the credit ledger and escrow system for tracking capability exchange economics.

Purpose: Credits create the economic incentive for capability sharing. Without escrow, there is no trust mechanism for the exchange.
Output: Working credit system with balance management, atomic escrow, settlement, refunds, and full test coverage.
</objective>

<execution_context>
@/Users/leyufounder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leyufounder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-dogfood/00-RESEARCH.md

<interfaces>
<!-- From src/types/index.ts — error class needed for credit operations -->
```typescript
export class AgentBnBError extends Error {
  constructor(message: string, public code: string) {
    super(message);
    this.name = 'AgentBnBError';
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build credit ledger with balance management</name>
  <files>src/credit/ledger.ts, src/credit/ledger.test.ts</files>
  <behavior>
    - Test: bootstrapAgent grants initial credits (100) and creates balance row
    - Test: bootstrapAgent is idempotent (calling twice does not double credits)
    - Test: getBalance returns 0 for unknown agent
    - Test: getBalance returns correct balance after bootstrap
    - Test: getTransactions returns chronological transaction log
    - Test: Transaction log entries have id, owner, amount, reason, reference_id, created_at
  </behavior>
  <action>
    1. Create `src/credit/ledger.ts` with:
       - `openCreditDb(path?: string)`: Opens SQLite DB with WAL mode, creates `credit_balances`, `credit_transactions`, `credit_escrow` tables (Pattern 4 from RESEARCH.md). Default `:memory:` for tests.
       - `bootstrapAgent(db, owner, amount?)`: INSERT OR IGNORE into credit_balances, then INSERT transaction with reason 'bootstrap'. Default amount 100. Idempotent via INSERT OR IGNORE.
       - `getBalance(db, owner)`: Returns current balance (0 if not found).
       - `getTransactions(db, owner, limit?)`: Returns transaction history, newest first.

    2. Create `src/credit/ledger.test.ts` with all behavior tests. Use `:memory:` DB per test.

    Use `db.transaction()` for bootstrap (Pattern 6). Throw `AgentBnBError('Insufficient credits', 'INSUFFICIENT_CREDITS')` where needed. JSDoc on all exports. `.js` extensions in imports.
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Documents/Github/agentbnb && pnpm test:run src/credit/ledger.test.ts</automated>
  </verify>
  <done>Bootstrap grants credits idempotently. Balance queries work. Transaction log is queryable. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build escrow hold, settle, and release operations</name>
  <files>src/credit/escrow.ts, src/credit/ledger.test.ts</files>
  <behavior>
    - Test: holdEscrow deducts from balance and creates escrow record with status 'held'
    - Test: holdEscrow with insufficient balance throws INSUFFICIENT_CREDITS
    - Test: holdEscrow prevents double-spend (two concurrent holds exceeding balance)
    - Test: settleEscrow transfers credits to capability owner and sets status 'settled'
    - Test: releaseEscrow refunds credits to requester and sets status 'released'
    - Test: settle/release on already-settled escrow throws ESCROW_ALREADY_SETTLED
    - Test: getEscrowStatus returns current escrow state
  </behavior>
  <action>
    1. Create `src/credit/escrow.ts` with:
       - `holdEscrow(db, owner, amount, cardId)`: Atomically check balance >= amount, deduct, create escrow record, log transaction. Returns escrowId. Uses `db.transaction()` (Pattern 6 from RESEARCH.md). Uses `UPDATE ... WHERE balance >= ?` with row count check to prevent race conditions (Pitfall 5).
       - `settleEscrow(db, escrowId, recipientOwner)`: Verify escrow status is 'held', credit recipient, update escrow to 'settled', log transactions for both parties.
       - `releaseEscrow(db, escrowId)`: Verify escrow status is 'held', refund to original owner, update escrow to 'released', log transaction.
       - `getEscrowStatus(db, escrowId)`: Returns escrow record or null.

    2. Add escrow tests to `src/credit/ledger.test.ts` in a new describe block "escrow".

    All operations wrapped in `db.transaction()`. Throw `AgentBnBError` with appropriate codes. No `any`.
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Documents/Github/agentbnb && pnpm test:run src/credit/ledger.test.ts</automated>
  </verify>
  <done>Escrow hold, settle, and release all work atomically. Double-spend prevented. Settled/released escrows cannot be re-settled. All tests green.</done>
</task>

</tasks>

<verification>
- `pnpm test:run src/credit/ledger.test.ts` passes all credit and escrow tests
- `pnpm typecheck` has no errors related to credit module
- Balance never goes negative in any test scenario
</verification>

<success_criteria>
- Bootstrap grants initial credits idempotently
- Escrow hold/settle/release are atomic (use db.transaction)
- Insufficient balance prevents hold
- Double-entry: every debit has a corresponding credit in transaction log
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/00-dogfood/00-02-SUMMARY.md`
</output>
