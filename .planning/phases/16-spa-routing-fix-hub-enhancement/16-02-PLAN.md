---
phase: 16-spa-routing-fix-hub-enhancement
plan: "02"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - hub/src/components/ui/marquee.tsx
  - hub/src/components/ui/number-flow.tsx
  - hub/src/components/ui/accordion.tsx
  - hub/src/components/ui/flickering-grid.tsx
  - hub/src/components/ui/line-chart.tsx
  - hub/src/components/ui/orbiting-circles.tsx
autonomous: true
requirements: [MAGICUI-01, MAGICUI-02, MAGICUI-03, MAGICUI-04, MAGICUI-05, MAGICUI-06]

must_haves:
  truths:
    - "NumberFlow component renders animated number transitions"
    - "Marquee component scrolls children horizontally with CSS animation"
    - "FlickeringGrid component renders a canvas-based flickering grid pattern"
    - "Accordion component expands/collapses content sections"
    - "LineChart component renders SVG line chart with smooth bezier curves"
    - "OrbitingCircles component renders children orbiting around a center point"
    - "All six components render without console errors in the Hub's dark theme"
  artifacts:
    - path: "hub/src/components/ui/number-flow.tsx"
      provides: "Animated number transition wrapper"
      exports: ["NumberFlowCell"]
    - path: "hub/src/components/ui/marquee.tsx"
      provides: "Horizontal/vertical marquee scrolling"
      exports: ["Marquee"]
    - path: "hub/src/components/ui/flickering-grid.tsx"
      provides: "Canvas-based flickering grid background"
      exports: ["FlickeringGrid"]
    - path: "hub/src/components/ui/accordion.tsx"
      provides: "Expandable accordion sections"
      exports: ["Accordion", "AccordionItem", "AccordionTrigger", "AccordionContent"]
    - path: "hub/src/components/ui/line-chart.tsx"
      provides: "SVG line chart with smooth curves"
      exports: ["LineChart"]
    - path: "hub/src/components/ui/orbiting-circles.tsx"
      provides: "Orbiting animation for child elements"
      exports: ["OrbitingCircles"]
  key_links:
    - from: "hub/src/components/ui/marquee.tsx"
      to: "hub/src/lib/cn.ts"
      via: "import"
      pattern: "import.*cn.*from.*lib/cn"
    - from: "hub/src/components/ui/flickering-grid.tsx"
      to: "hub/src/lib/color.ts"
      via: "import for canvas rendering"
      pattern: "import.*getRGBA.*colorWithOpacity.*from.*lib/color"
    - from: "hub/src/components/ui/line-chart.tsx"
      to: "motion"
      via: "npm dependency for SVG animations"
      pattern: "import.*motion.*from.*motion/react"
    - from: "hub/src/components/ui/accordion.tsx"
      to: "@radix-ui/react-accordion"
      via: "npm dependency for accessible accordion primitive"
      pattern: "import.*AccordionPrimitive.*from.*@radix-ui/react-accordion"
---

<objective>
Extract and adapt all six Magic UI components into the Hub's component library at hub/src/components/ui/.

Purpose: These components will be used in Phase 17 (below-fold sections: Marquee for Compatible With, Accordion for FAQ) and throughout the Hub for visual polish (FlickeringGrid background, NumberFlow for stats, LineChart for earnings, OrbitingCircles for decoration).
Output: Six self-contained React component files in hub/src/components/ui/
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-spa-routing-fix-hub-enhancement/16-01-PLAN.md

@hub/tailwind.config.js
@hub/src/index.css
@hub/src/lib/cn.ts
@hub/src/lib/color.ts

<interfaces>
<!-- From Plan 01 — cn utility (hub/src/lib/cn.ts) -->
```typescript
export function cn(...inputs: ClassValue[]): string;
```

<!-- From Plan 01 — color utilities (hub/src/lib/color.ts) -->
```typescript
export function getRGBA(cssColor: string, fallback?: string): string;
export function colorWithOpacity(color: string, opacity: number): string;
```

<!-- Hub design tokens from tailwind.config.js -->
```
bg: '#08080C'
accent: '#10B981'
text-primary: 'rgba(255, 255, 255, 0.92)'
text-secondary: 'rgba(255, 255, 255, 0.55)'
border: 'rgba(255, 255, 255, 0.06)'
```

<!-- Tailwind animations available from Plan 01 -->
```
animate-marquee, animate-marquee-vertical, animate-accordion-down, animate-accordion-up, animate-orbit
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Marquee, NumberFlow, and Accordion components</name>
  <files>hub/src/components/ui/marquee.tsx, hub/src/components/ui/number-flow.tsx, hub/src/components/ui/accordion.tsx</files>
  <action>
Create the hub/src/components/ui/ directory if it does not exist.

**1. hub/src/components/ui/marquee.tsx** (MAGICUI-02)
Adapt from magicuidesign template's src/components/ui/marquee.tsx:
- Remove `"use client"` directive (Vite, not Next.js)
- Change import from `@/lib/utils` to `../../lib/cn.js`
- Keep all props: className, reverse, pauseOnHover, children, vertical, repeat
- Keep the CSS custom property pattern: `[--duration:40s] [--gap:1rem]`
- Keep `animate-marquee` and `animate-marquee-vertical` class references (keyframes defined in tailwind.config.js by Plan 01)
- Export as named export: `export function Marquee`
- Add JSDoc comment explaining the component

**2. hub/src/components/ui/number-flow.tsx** (MAGICUI-01)
Create a thin wrapper around @number-flow/react that matches the Hub's styling:
```typescript
import NumberFlow from '@number-flow/react';
import { cn } from '../../lib/cn.js';

export interface NumberFlowCellProps {
  value: number;
  className?: string;
  format?: Intl.NumberFormatOptions;
}

/**
 * Animated number transition component.
 * Wraps @number-flow/react with Hub-compatible styling defaults.
 */
export function NumberFlowCell({ value, className, format }: NumberFlowCellProps): JSX.Element {
  return (
    <NumberFlow
      value={value}
      className={cn('font-mono tabular-nums', className)}
      transformTiming={{ duration: 700, easing: 'ease-out' }}
      format={format ?? { useGrouping: true, minimumIntegerDigits: 1 }}
    />
  );
}
```

**3. hub/src/components/ui/accordion.tsx** (MAGICUI-04)
Adapt from magicuidesign template's src/components/ui/accordion.tsx:
- Remove `"use client"` directive
- Change import from `@/lib/utils` to `../../lib/cn.js`
- Keep the 4 exports: Accordion, AccordionItem, AccordionTrigger, AccordionContent
- Restyle for Hub dark theme:
  - AccordionItem border: `border-white/[0.06]` (hub-border token) instead of default `border-b`
  - AccordionTrigger: replace shadcn focus ring classes with Hub-compatible ones. Use `text-hub-text-primary hover:text-hub-text-secondary` for text colors. Remove `hover:underline`. Keep the chevron rotation `[&[data-state=open]>svg]:rotate-180`.
  - AccordionTrigger chevron: `text-hub-text-muted` instead of `text-muted-foreground`
  - AccordionContent: uses `animate-accordion-down` / `animate-accordion-up` (defined in tailwind config from Plan 01). Add `text-hub-text-secondary` for content text.
- Use `ChevronDown` from `lucide-react` (already a Hub dependency) instead of `ChevronDownIcon`
- Add JSDoc comments
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb/hub && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Three component files exist in hub/src/components/ui/ — marquee.tsx, number-flow.tsx, accordion.tsx. All compile with zero TypeScript errors. Marquee uses CSS animation classes, NumberFlow wraps @number-flow/react, Accordion uses @radix-ui primitives with Hub dark theme styling.</done>
</task>

<task type="auto">
  <name>Task 2: Extract FlickeringGrid, LineChart, and OrbitingCircles components</name>
  <files>hub/src/components/ui/flickering-grid.tsx, hub/src/components/ui/line-chart.tsx, hub/src/components/ui/orbiting-circles.tsx</files>
  <action>
**1. hub/src/components/ui/flickering-grid.tsx** (MAGICUI-03)
Adapt from magicuidesign template's src/components/ui/flickering-grid.tsx:
- Remove `"use client"` directive
- Change `@/lib/utils` cn import to `../../lib/cn.js`
- Import getRGBA and colorWithOpacity from `../../lib/color.js` instead of `@/lib/utils`
- Remove the `text`, `textColor`, `fontSize`, `fontWeight` props and all text-mask canvas logic (not needed for background texture use case — simplifies the component significantly). Keep only: squareSize, gridGap, flickerChance, color, width, height, className, maxOpacity.
- Simplify drawGrid: remove the entire maskCanvas/text-rendering block. Just draw colored squares with random opacity flickering.
- Keep IntersectionObserver for performance (only animate when in view)
- Keep ResizeObserver for responsive sizing
- Default color to `#10B981` (hub-accent, emerald) instead of `#B4B4B4`
- Default maxOpacity to `0.08` (very subtle background texture for dark theme)
- Add JSDoc comment

**2. hub/src/components/ui/line-chart.tsx** (MAGICUI-05)
Adapt the LineChart function from magicuidesign template's src/components/third-bento-animation.tsx (lines 17-196). Extract ONLY the LineChart component, NOT ThirdBentoAnimation or NumberFlowCounter:
- Remove `"use client"` directive
- Import `motion` from `motion/react` (already installed by Plan 01)
- Import getRGBA and colorWithOpacity from `../../lib/color.js`
- Keep all SVG rendering logic: createSmoothPath bezier curves, gradient fill, animated line, center dot, pulsing waves
- Keep the shouldAnimate and startAnimationDelay props for scroll-triggered animations
- Add a `useInView` hook (from motion/react) so the chart self-animates when scrolled into view, making shouldAnimate optional. If shouldAnimate is explicitly passed, use that; otherwise derive from IntersectionObserver.
- Export as named export: `export function LineChart`
- Add JSDoc comment

**3. hub/src/components/ui/orbiting-circles.tsx** (MAGICUI-06)
Adapt from magicuidesign template's src/components/ui/orbiting-circle.tsx:
- Remove `"use client"` directive
- Change `@/lib/utils` cn import to `../../lib/cn.js`
- Import `motion, useInView, cubicBezier` from `motion/react`
- Keep all orbital animation logic: CSS custom properties (--duration, --radius, --angle, --icon-size), animate-orbit class
- Restyle the orbital path ring for Hub dark theme:
  - Replace `dark:border-[rgba(249,250,251,0.07)]` with `border-white/[0.06]` (matches hub-border)
  - Replace gradient classes with Hub-compatible: `from-white/[0.03] via-transparent`
  - Remove all light-mode-only classes (the Hub is dark-only)
- Keep the spring entrance animation with staggered delays
- Type the props interface properly (extend HTMLMotionProps or use a custom interface without the framer-motion HTMLMotionProps if it causes import issues — use a plain interface with className, children, reverse, duration, delay, radius, path, iconSize, speed, index, startAnimationDelay, once)
- Export as named export: `export function OrbitingCircles`
- Add JSDoc comment

For ALL three components:
- Use .js extension in relative imports (ESM compatibility)
- No `any` types — use `unknown` and narrow per project conventions
- Ensure all public functions have JSDoc comments per CLAUDE.md
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb/hub && npx tsc --noEmit 2>&1 | tail -10 && echo "---" && ls -la src/components/ui/ 2>&1</automated>
  </verify>
  <done>Six component files exist in hub/src/components/ui/: marquee.tsx, number-flow.tsx, accordion.tsx, flickering-grid.tsx, line-chart.tsx, orbiting-circles.tsx. All compile with zero TypeScript errors. FlickeringGrid uses canvas with color-bits, LineChart uses motion/react for SVG animations, OrbitingCircles uses motion/react with CSS orbit keyframe. All styled for Hub dark theme.</done>
</task>

</tasks>

<verification>
1. `ls hub/src/components/ui/*.tsx | wc -l` returns 6
2. `cd hub && npx tsc --noEmit` passes with 0 errors
3. `cd hub && npx vite build` succeeds (production bundle builds)
4. Each component file has a JSDoc comment and named export
5. No `"use client"` directives in any component (Vite, not Next.js)
6. No `@/lib/utils` imports (all replaced with relative `../../lib/cn.js` or `../../lib/color.js`)
7. All existing hub tests pass: `cd hub && npx vitest run`
</verification>

<success_criteria>
- Six Magic UI components exist in hub/src/components/ui/ and compile without errors
- Marquee: horizontal/vertical scrolling with CSS animation, pauseOnHover support
- NumberFlow: animated number transitions via @number-flow/react
- FlickeringGrid: canvas-based subtle flickering grid (no text mask, simplified)
- Accordion: radix-ui based expand/collapse with Hub dark theme styling
- LineChart: SVG line chart with smooth bezier curves and motion animations
- OrbitingCircles: children orbit around center with CSS keyframe + motion entrance
- Production build (vite build) succeeds with all six components included
- All existing Hub tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-spa-routing-fix-hub-enhancement/16-02-SUMMARY.md`
</output>
