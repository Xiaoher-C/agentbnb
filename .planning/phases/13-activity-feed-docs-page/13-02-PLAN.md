---
phase: 13-activity-feed-docs-page
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - hub/src/components/CopyButton.tsx
  - hub/src/lib/docs-content.tsx
  - hub/src/components/DocsPage.tsx
  - hub/src/main.tsx
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]

must_haves:
  truths:
    - "The Docs page shows a Getting Started guide explaining what AgentBnB is and how to bootstrap"
    - "The Docs page shows install commands for Claude Code, OpenClaw, Antigravity, and CLI with working copy buttons"
    - "The Docs page shows Capability Card schema reference with field descriptions"
    - "The Docs page shows API endpoint reference covering all public endpoints"
    - "All content is static TypeScript JSX — no network requests, no markdown processing"
  artifacts:
    - path: "hub/src/components/CopyButton.tsx"
      provides: "Reusable copy-to-clipboard button with checkmark feedback"
      contains: "CopyButton"
    - path: "hub/src/lib/docs-content.tsx"
      provides: "Static documentation sections as TypeScript JSX data"
      contains: "DOCS_SECTIONS"
    - path: "hub/src/components/DocsPage.tsx"
      provides: "Documentation page with sidebar navigation and content area"
      contains: "DocsPage"
  key_links:
    - from: "hub/src/components/DocsPage.tsx"
      to: "hub/src/lib/docs-content.tsx"
      via: "imports DOCS_SECTIONS array"
      pattern: "DOCS_SECTIONS"
    - from: "hub/src/lib/docs-content.tsx"
      to: "hub/src/components/CopyButton.tsx"
      via: "uses CopyButton in install command sections"
      pattern: "CopyButton"
    - from: "hub/src/main.tsx"
      to: "hub/src/components/DocsPage.tsx"
      via: "route element import"
      pattern: "DocsPage"
---

<objective>
Build the embedded Docs page with 4 sections (Getting Started, Install Commands, Card Schema, API Reference) using static TypeScript JSX data and a reusable CopyButton component.

Purpose: Visitors can learn how to use AgentBnB without leaving the Hub. Install commands with copy buttons reduce friction to zero.
Output: Working /hub/#/docs page with sidebar navigation, 4 content sections, and copyable install commands.
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-activity-feed-docs-page/13-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From hub/src/components/GetStartedCTA.tsx (copy button pattern to extract):
```typescript
// Reuse this exact clipboard pattern in CopyButton.tsx
const [copied, setCopied] = useState(false);
const handleCopy = async (): Promise<void> => {
  try {
    await navigator.clipboard.writeText(CLI_COMMAND);
    setCopied(true);
    setTimeout(() => { setCopied(false); }, 1500);
  } catch {
    // Clipboard API unavailable — no-op
  }
};
// Uses lucide-react Copy and Check icons
import { Copy, Check } from 'lucide-react';
```

From hub/src/main.tsx (lines 69-76 — replace this placeholder):
```typescript
{
  path: 'docs',
  element: (
    <div className="text-hub-text-muted py-12 text-center">
      Docs — Phase 13
    </div>
  ),
},
```

From CLAUDE.md (Capability Card schema for DOCS-03):
```typescript
interface CapabilityCard {
  spec_version: '1.0';
  id: string;
  owner: string;
  name: string;
  description: string;
  level: 1 | 2 | 3;       // Atomic | Pipeline | Environment
  skills?: Skill[];
  inputs: IOSchema[];
  outputs: IOSchema[];
  pricing: { credits_per_call: number; credits_per_minute?: number; free_tier?: number };
  availability: { online: boolean; schedule?: string };
  powered_by?: PoweredBy[];
  metadata?: { apis_used?: string[]; avg_latency_ms?: number; success_rate?: number; tags?: string[] };
}
```

API endpoints for DOCS-04 (all public endpoints from server.ts):
```
Public:
  GET  /health                          — Health check
  GET  /cards                           — List all capability cards (paginated)
  GET  /cards/:id                       — Get single card by ID
  GET  /cards?q=search_term             — Full-text search via FTS5
  GET  /api/agents                      — List agents ranked by reputation
  GET  /api/agents/:owner               — Agent profile with skills and activity
  GET  /api/activity                    — Public activity feed (supports ?since, ?limit)

Authenticated (Bearer token):
  GET  /me                              — Owner identity and credit balance
  GET  /requests                        — Request log (supports ?limit, ?since)
  GET  /draft                           — Auto-generated draft cards from API keys
  POST /cards/:id/toggle-online         — Toggle card online/offline
  PATCH /cards/:id                      — Update card description/pricing
  GET  /me/pending-requests             — List Tier 3 pending approvals
  POST /me/pending-requests/:id/approve — Approve pending request
  POST /me/pending-requests/:id/reject  — Reject pending request
```

Install commands from SKILL.md and project docs:
```
Claude Code:  npx agentbnb init
CLI:          npm install -g agentbnb && agentbnb init
OpenClaw:     openclaw install agentbnb
Antigravity:  ag skill add agentbnb
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CopyButton component, docs-content data, and DocsPage component</name>
  <files>hub/src/components/CopyButton.tsx, hub/src/lib/docs-content.tsx, hub/src/components/DocsPage.tsx</files>
  <action>
1. Create `hub/src/components/CopyButton.tsx` — reusable copy-to-clipboard button:
- Props: `{ text: string; label?: string }` — text is what to copy, label is optional display text
- Extract the copy pattern from GetStartedCTA.tsx: useState for `copied`, navigator.clipboard.writeText, 1500ms timeout for check icon
- Use lucide-react `Copy` and `Check` icons (already installed)
- Styling: inline button with hover state, icon swaps to green Check on success
- Wrap the text in a monospace code span (font-mono text-emerald-400) with the Copy button beside it
- Background: `bg-black/[0.3] rounded-md px-3 py-2` (matching GetStartedCTA pattern)
- Export as default

2. Create `hub/src/lib/docs-content.tsx` (use .tsx extension since it contains JSX):

Export a `DocSection` interface and `DOCS_SECTIONS` array with 4 sections:

```typescript
export interface DocSection {
  id: string;
  title: string;
  content: React.ReactNode;
}
```

**Section 1: "Getting Started"** (DOCS-01)
- Brief explanation: "AgentBnB is a P2P agent capability sharing protocol. Agent owners publish what their agents can do (Capability Cards) and request capabilities from others."
- 3-step quick start: (1) Install via CLI, (2) Publish a capability card, (3) Discover and request capabilities
- Each step shows the CLI command with a CopyButton
- Commands: `npx agentbnb init`, `agentbnb publish`, `agentbnb discover`
- Mention the 50 free credits on init
- Style headings with `text-lg font-semibold text-hub-text-primary mb-3`, paragraphs with `text-hub-text-secondary text-sm leading-relaxed`

**Section 2: "Install"** (DOCS-02)
- 4 install methods, each with tool name, one-line description, and CopyButton:
  - Claude Code: `npx agentbnb init` — "Bootstrap via Claude Code skill"
  - CLI (direct): `npm install -g agentbnb && agentbnb init` — "Global CLI install"
  - OpenClaw: `openclaw install agentbnb` — "OpenClaw skill registry"
  - Antigravity: `ag skill add agentbnb` — "Antigravity skill manager"
- Each install method in a row: tool name (font-medium), description (text-muted), CopyButton with command
- Use a grid or stacked card layout with `bg-white/[0.03] rounded-lg p-4` for each method

**Section 3: "Card Schema"** (DOCS-03)
- Show the Capability Card v2.0 interface fields in a table-like layout
- Fields to document: spec_version, id, owner, name, description, level (1/2/3), skills[], inputs, outputs, pricing, availability, powered_by, metadata
- For each field: name (font-mono emerald), type, and brief description
- Style as a definition list with alternating subtle row backgrounds

**Section 4: "API Reference"** (DOCS-04)
- Two sections: "Public Endpoints" and "Authenticated Endpoints"
- Public endpoints:
  - GET /health — Health check (returns { status: 'ok' })
  - GET /cards — List capability cards (query: ?q, ?limit, ?offset)
  - GET /cards/:id — Get single card
  - GET /api/agents — Ranked agent list
  - GET /api/agents/:owner — Agent profile with skills + activity
  - GET /api/activity — Public activity feed (query: ?since, ?limit)
- Authenticated endpoints (Bearer token in Authorization header):
  - GET /me — Owner identity + credit balance
  - GET /requests — Request log (query: ?limit, ?since)
  - GET /draft — Auto-draft cards from API keys
  - POST /cards/:id/toggle-online — Toggle online status
  - PATCH /cards/:id — Update description/pricing
  - GET /me/pending-requests — Tier 3 pending approvals
  - POST /me/pending-requests/:id/approve — Approve request
  - POST /me/pending-requests/:id/reject — Reject request
- Each endpoint: method badge (GET=emerald, POST=blue, PATCH=yellow), path in font-mono, brief description
- Style method badges as small pills with appropriate colors

All sections use Tailwind classes directly (no @tailwindcss/typography prose). Use the hub dark theme tokens: text-hub-text-primary, text-hub-text-secondary, text-hub-text-muted, bg-hub-surface, border-white/[0.08].

3. Create `hub/src/components/DocsPage.tsx`:
- Import `DOCS_SECTIONS` from `../lib/docs-content.js`
- State: `activeSection` (string, defaults to 'getting-started')
- Layout: flex row with sidebar (w-48 shrink-0, sticky top) + content area (flex-1 min-w-0)
- Sidebar: map DOCS_SECTIONS to buttons, active button gets emerald text + left border, inactive gets muted text
- Content: render the active section's content
- On mobile (< 768px): sidebar becomes horizontal scroll tabs at top (flex-row overflow-x-auto)
- Export as default
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb/hub && npx tsc --noEmit</automated>
  </verify>
  <done>CopyButton component exists with clipboard + checkmark feedback. docs-content.tsx exports 4 sections (Getting Started, Install, Card Schema, API Reference) with correct content. DocsPage renders sidebar nav + content area. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DocsPage into router</name>
  <files>hub/src/main.tsx</files>
  <action>
1. Update `hub/src/main.tsx`:
- Add import at top (after other component imports): `import DocsPage from './components/DocsPage.js';`
- Replace the docs placeholder div (the route entry at path 'docs', lines 69-76) with: `{ path: 'docs', element: <DocsPage /> }`
- NOTE: Plan 13-01 already replaced the activity placeholder. This task only touches the docs route entry. Read the current file state first to confirm the activity route was already updated.

2. Verify both placeholder divs are now replaced:
- `path: 'activity'` should have `element: <ActivityFeed />`
- `path: 'docs'` should have `element: <DocsPage />`
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb/hub && npx tsc --noEmit</automated>
  </verify>
  <done>Both placeholder divs in main.tsx replaced with real components. /hub/#/docs renders the DocsPage. /hub/#/activity renders ActivityFeed (from Plan 01). TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `cd hub && npx tsc --noEmit` — TypeScript compiles clean with all new components
2. Navigate to /hub/#/docs — shows Getting Started section by default
3. Click each sidebar nav item — shows Install, Card Schema, API Reference sections
4. Click copy button on an install command — clipboard contains the correct command, icon changes to checkmark
5. No network requests made by the docs page (all content is static)
</verification>

<success_criteria>
- /hub/#/docs shows 4 navigable sections: Getting Started, Install, Card Schema, API Reference
- Install section shows commands for Claude Code, OpenClaw, Antigravity, and CLI with working copy buttons
- Card Schema section documents all CapabilityCard v2.0 fields
- API Reference section lists all public and authenticated endpoints with method badges
- All content is static TypeScript JSX — no fetch calls, no markdown processing
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-activity-feed-docs-page/13-02-SUMMARY.md`
</output>
