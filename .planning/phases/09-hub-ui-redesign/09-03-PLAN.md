---
phase: 09-hub-ui-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - hub/src/App.tsx
  - hub/src/components/StatsBar.tsx
  - hub/src/components/SearchFilter.tsx
autonomous: true
requirements:
  - HUI-04
  - HUI-05
  - HUI-06

must_haves:
  truths:
    - "Header shows 'AgentBnB' at 24px semibold white 92%, no subtitle"
    - "Stats numbers render at 32px JetBrains Mono in emerald green"
    - "Stats labels render at 14px in white 40%"
    - "A radial gradient ambient glow (600px, accent color, 0.08 opacity) appears behind the stats area"
    - "Search bar is full-width, 48px height, rounded-xl, ghost style with transparent bg and border"
    - "Level and category dropdowns use the dark ghost style"
    - "Online-only toggle uses the dark design tokens"
    - "Tab switcher uses pill style with active fill rgba(255,255,255,0.08), no underline"
  artifacts:
    - path: "hub/src/components/StatsBar.tsx"
      provides: "Premium stats bar with large mono numbers and ambient glow"
      min_lines: 30
    - path: "hub/src/components/SearchFilter.tsx"
      provides: "Redesigned search + filter bar with ghost styling"
      min_lines: 40
    - path: "hub/src/App.tsx"
      provides: "Updated header, tabs, and ambient glow"
      contains: "radial-gradient"
  key_links:
    - from: "hub/src/App.tsx"
      to: "hub/src/components/StatsBar.tsx"
      via: "Props: agentsOnline, totalCapabilities, totalExchanges"
      pattern: "StatsBar"
    - from: "hub/src/App.tsx"
      to: "hub/src/components/SearchFilter.tsx"
      via: "Props: query, level, category, onlineOnly + setters"
      pattern: "SearchFilter"
---

<objective>
Redesign the header area (logo, stats bar with ambient glow), search/filter bar, and tab switcher to match the premium dark SaaS aesthetic with large monospace stats, ghost-style inputs, and pill-style tabs.

Purpose: The header and filter area is the first thing users see. It must communicate premium quality instantly. The ambient glow behind stats creates the "alive" atmospheric feel described in CONTEXT.md.
Output: Updated StatsBar (32px mono numbers + glow), SearchFilter (48px ghost inputs), and App.tsx (header, tabs, glow).
</objective>

<execution_context>
@/Users/xiaoher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xiaoher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-hub-ui-redesign/09-CONTEXT.md
@.planning/phases/09-hub-ui-redesign/09-01-SUMMARY.md

@hub/src/App.tsx
@hub/src/components/StatsBar.tsx
@hub/src/components/SearchFilter.tsx
@hub/src/hooks/useCards.ts
@hub/src/types.ts

<interfaces>
<!-- From useCards hook (unchanged): -->
```typescript
interface UseCardsResult {
  cards: HubCard[];
  total: number;
  loading: boolean;
  error: string | null;
  query: string;
  setQuery: (q: string) => void;
  level: number | null;
  setLevel: (l: number | null) => void;
  category: string | null;
  setCategory: (c: string | null) => void;
  onlineOnly: boolean;
  setOnlineOnly: (v: boolean) => void;
  availableCategories: Category[];
  retry: () => void;
  agentsOnline: number;
  totalCapabilities: number;
  totalExchanges: number;
}
```

<!-- StatsBar props (unchanged interface, content redesigned): -->
```typescript
interface StatsBarProps {
  agentsOnline: number;
  totalCapabilities: number;
  totalExchanges: number;
}
```

<!-- SearchFilter props (unchanged interface, visual redesigned): -->
```typescript
interface SearchFilterProps {
  query: string;
  onQueryChange: (q: string) => void;
  level: number | null;
  onLevelChange: (l: number | null) => void;
  category: string | null;
  onCategoryChange: (c: string | null) => void;
  onlineOnly: boolean;
  onOnlineOnlyChange: (v: boolean) => void;
  availableCategories: Category[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatsBar redesign with ambient glow</name>
  <files>hub/src/components/StatsBar.tsx</files>
  <action>
Rewrite StatsBar to match CONTEXT.md premium design:

**Layout:**
- The component wraps stats in a container with relative positioning
- Behind the stats, render an ambient glow: a `div` with `absolute` positioning, centered, ~600px wide, ~200px tall, with `background: radial-gradient(ellipse at center, rgba(16, 185, 129, 0.08) 0%, transparent 70%)`. This creates the soft emerald halo behind the stats. Set `pointer-events: none` and `z-index: 0` on the glow div. Stats content has `z-index: 10` / `relative`.

**Stats display:**
- Each stat: large number + small label stacked vertically
- Number: 32px, `font-mono font-semibold`, `text-hub-accent` (emerald green). Use `text-[32px] leading-tight`.
- Label: 14px, `text-hub-text-muted` (white 40%). Use `text-sm`.
- Three stats in a row: "Agents Online", "Capabilities", "Exchanges"
- Use flexbox with `justify-center gap-12` to space them evenly
- Between stats, use a thin separator: `w-px h-8 bg-white/[0.06]`

**Note:** The count-up animation is deferred to Plan 04 (HUI-07 polish). For now, render the raw numbers. The component must accept the same props interface `{ agentsOnline, totalCapabilities, totalExchanges }`.

Keep the JSDoc comment and props interface from the existing file.
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb/hub && npx tsc --noEmit 2>&1 | tail -5; echo "exit: $?"</automated>
  </verify>
  <done>StatsBar renders three stats with 32px JetBrains Mono emerald numbers, 14px white-40% labels, and a 600px radial gradient ambient glow behind them. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: SearchFilter redesign + App.tsx header and tabs</name>
  <files>hub/src/components/SearchFilter.tsx, hub/src/App.tsx</files>
  <action>
**SearchFilter.tsx** — Full visual redesign, same props interface:

1. **Search bar:** Full width (`w-full`), 48px height (`h-12`), rounded-xl, ghost style:
   - `bg-transparent border border-hub-border rounded-xl px-4 h-12 text-hub-text-primary placeholder:text-hub-text-tertiary`
   - Focus: `focus:outline-none focus:border-hub-border-hover focus:ring-1 focus:ring-hub-border-hover`
   - Search icon (lucide `Search`) positioned left, `text-hub-text-tertiary`
   - The search input takes the full width of the container

2. **Filter row below search:** Level dropdown, category dropdown, online-only toggle in a flex row with gap-3, margin-top 12px (`mt-3`).

3. **Dropdowns:** Ghost style matching the search bar:
   - `bg-transparent border border-hub-border rounded-lg px-3 h-10 text-sm text-hub-text-secondary`
   - Focus: same ring pattern as search
   - `appearance-none` + custom dropdown arrow via background SVG or pseudo-element if desired (optional, standard select arrow is acceptable)

4. **Online-only toggle:** Use the design token colors:
   - Label text: `text-sm text-hub-text-secondary`
   - Checkbox: custom-styled or use `accent-hub-accent` (Tailwind `accent-emerald-500`)

Remove all `bg-slate-800`, `border-slate-700`, `text-slate-*`, `focus:ring-indigo-500` references. Everything uses `hub-*` tokens.

**App.tsx** — Update header and tab navigation:

1. **Header:**
   - "AgentBnB" logo: `text-2xl font-semibold` (24px), `text-hub-text-primary` (white 92%)
   - Remove the subtitle "Browse available agent capabilities"
   - Keep the Disconnect button for authenticated users, but restyle: `text-xs text-hub-text-tertiary hover:text-hub-text-secondary`
   - Remove `bg-slate-900` from the root div, use `bg-hub-bg min-h-screen`

2. **Tab navigation — Pill switcher:**
   - Remove the `border-b border-slate-700` underline
   - Wrap tabs in a container with `flex gap-1 bg-white/[0.04] rounded-lg p-1` (subtle pill container)
   - Each tab button: `px-4 py-1.5 rounded-md text-sm font-medium transition-colors`
   - Active tab: `bg-white/[0.08] text-hub-text-primary`
   - Inactive tab: `bg-transparent text-hub-text-muted hover:text-hub-text-secondary`
   - No underlines, no borders on individual tabs — just fill difference

3. **Main area:** Update `bg-slate-900` to `bg-hub-bg`, remove any lingering slate classes.

4. **Important:** This plan may run in parallel with plan 02 (modal). Both modify App.tsx. Plan 02 adds modal state + CardModal. Plan 03 updates header + tabs. If plan 02 runs first, preserve its CardModal wiring. If plan 03 runs first, plan 02 will add modal state on top. The changes are in different sections of App.tsx (header area vs bottom/state), so they should merge cleanly. To be safe, read the current App.tsx state before writing.
  </action>
  <verify>
    <automated>cd /Users/xiaoher/Documents/GitHub/agentbnb/hub && npx tsc --noEmit 2>&1 | tail -5 && pnpm run build 2>&1 | tail -10; echo "exit: $?"</automated>
  </verify>
  <done>SearchFilter renders full-width 48px ghost search bar with ghost dropdowns and emerald toggle. App.tsx header shows "AgentBnB" at 24px semibold, pill-style tab switcher with active fill, no underlines. All components use hub-* design tokens. TypeScript compiles and build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd hub && npx tsc --noEmit` — No type errors
2. `cd hub && pnpm run build` — Build succeeds
3. Visual: Stats show 32px emerald mono numbers with glow behind
4. Visual: Search bar is full-width ghost style, 48px tall
5. Visual: Tabs are pill-style with subtle fill, no underline
</verification>

<success_criteria>
- StatsBar shows 32px JetBrains Mono green numbers with radial gradient glow
- SearchFilter is full-width 48px ghost style with matching dropdowns
- Tab switcher is pill-style (active: rgba(255,255,255,0.08) fill, no underline)
- Header shows "AgentBnB" at 24px, no subtitle
- No slate/indigo legacy colors remain in modified files
- TypeScript compiles and Vite build passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-hub-ui-redesign/09-03-SUMMARY.md`
</output>
