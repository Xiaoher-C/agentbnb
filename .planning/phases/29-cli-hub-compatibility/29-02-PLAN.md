---
phase: 29-cli-hub-compatibility
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/cli/cli-compat.test.ts
  - src/registry/server-compat.test.ts
autonomous: true
requirements: [COMPAT-01, COMPAT-02, COMPAT-03, COMPAT-04]

must_haves:
  truths:
    - "An agent with no registryUrl in config uses LocalCreditLedger for all credit operations"
    - "Local gateway handles LAN P2P exchanges with local escrow unchanged"
    - "Existing credit.db data is not migrated or destroyed"
    - "All 739+ existing tests pass without modification"
  artifacts:
    - path: "src/cli/cli-compat.test.ts"
      provides: "Compatibility tests for CLI credit operations in local-only mode"
      min_lines: 40
    - path: "src/registry/server-compat.test.ts"
      provides: "Compatibility tests for Hub endpoints in local-only mode"
      min_lines: 30
  key_links:
    - from: "src/cli/index.ts"
      to: "src/credit/create-ledger.ts"
      via: "createLedger with creditDbPath (no registryUrl)"
      pattern: "createLedger.*creditDbPath"
    - from: "src/registry/server.ts"
      to: "src/credit/create-ledger.ts"
      via: "createLedger with db (direct mode)"
      pattern: "createLedger.*db"
---

<objective>
Verify backward compatibility: agents without registryUrl config continue using local SQLite credits, and the full test suite passes.

Purpose: COMPAT requirements ensure that the CreditLedger migration from Plan 01 does not break any existing functionality for local-only or LAN agents.
Output: New compatibility test files + full test suite green.
</objective>

<execution_context>
@/Users/leyufounder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leyufounder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-cli-hub-compatibility/29-01-SUMMARY.md

<interfaces>
From src/credit/create-ledger.ts:
```typescript
export function createLedger(opts: CreateLedgerOptions): CreditLedger;
// When no registryUrl: createLedger({ creditDbPath }) returns LocalCreditLedger
// When db provided: createLedger({ db }) returns RegistryCreditLedger in direct mode
```

From src/credit/local-credit-ledger.ts:
```typescript
export class LocalCreditLedger implements CreditLedger { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backward compatibility tests for CLI and Hub</name>
  <files>src/cli/cli-compat.test.ts, src/registry/server-compat.test.ts</files>
  <behavior>
  CLI compat tests (src/cli/cli-compat.test.ts):
  - Test: createLedger with creditDbPath and no registryUrl returns LocalCreditLedger instance
  - Test: LocalCreditLedger.getBalance returns correct balance from local credit.db
  - Test: LocalCreditLedger.grant + getBalance round-trip works
  - Test: LocalCreditLedger.hold + settle flow works for local escrow
  - Test: publish validation rejects credits_per_call=0 (CLI-04 compat)

  Hub compat tests (src/registry/server-compat.test.ts):
  - Test: GET /me with creditDb returns balance via CreditLedger (direct mode) — same shape as before
  - Test: GET /me/transactions with creditDb returns history — same shape as before
  - Test: GET /me without creditDb returns balance=0 — unchanged behavior
  </behavior>
  <action>
  Create src/cli/cli-compat.test.ts:
  - Import createLedger, LocalCreditLedger from '../credit/create-ledger.js'
  - Import openCreditDb, bootstrapAgent from '../credit/ledger.js'
  - Use tmp directory for test databases (vitest beforeEach/afterEach)
  - Write tests verifying local-only credit flow works end-to-end through CreditLedger interface
  - Test the minimum price validation logic (can test as a pure function if extracted, or test publish command output)

  Create src/registry/server-compat.test.ts:
  - Import createRegistryServer from '../registry/server.js'
  - Use server.inject() for HTTP assertions (Fastify test pattern used throughout codebase)
  - Create test with creditDb (openCreditDb on temp path), bootstrapAgent, then verify /me and /me/transactions responses match expected shape
  - Create test without creditDb: verify /me returns balance=0
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Github/agentbnb && npx vitest run src/cli/cli-compat.test.ts src/registry/server-compat.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
  - All compat tests pass — local-only agents have unchanged behavior
  - CreditLedger in local mode produces identical results to direct ledger.ts calls
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify no regressions</name>
  <files></files>
  <action>
  Run the complete test suite: `npx vitest run --reporter=verbose`

  Expected: All 739+ tests pass. If any test fails:
  1. Identify if the failure is caused by Plan 01 changes (CLI/Hub CreditLedger wiring)
  2. Fix the failing test or the source code — do NOT skip or delete tests
  3. Common failure modes to watch for:
     - Tests that mock getBalance/getTransactions directly — they may need to also mock createLedger
     - Tests that check exact console output from `init` or `status` — update expected strings
     - Tests that create registry servers without creditDb — should still work (the guard handles it)

  After all tests pass, verify COMPAT-03 (no destructive migration):
  - Confirm no migration scripts were added
  - Confirm credit.db schema is unchanged (no ALTER TABLE, no new tables added by Plan 01)
  - The existing openCreditDb/bootstrapAgent path is preserved
  </action>
  <verify>
    <automated>cd /Users/leyufounder/Github/agentbnb && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
  - All 739+ existing tests pass
  - New compat tests pass
  - No destructive migration to credit.db
  - COMPAT-01 through COMPAT-04 verified
  </done>
</task>

</tasks>

<verification>
```bash
# Full test suite
cd /Users/leyufounder/Github/agentbnb && npx vitest run 2>&1 | tail -10

# Verify no new migration files
git diff --name-only HEAD | grep -i migrat || echo "No migration files"

# Verify credit.db schema unchanged
grep -r "ALTER TABLE\|CREATE TABLE.*credit" src/credit/ledger.ts | head -5
```
</verification>

<success_criteria>
- All 739+ existing tests pass without modification
- New compat tests verify local-only mode works through CreditLedger
- No destructive database migration
- Local gateway P2P exchanges work with local escrow
</success_criteria>

<output>
After completion, create `.planning/phases/29-cli-hub-compatibility/29-02-SUMMARY.md`
</output>
