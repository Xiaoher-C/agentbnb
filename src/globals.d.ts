/**
 * Build-time constant injected by tsup (esbuild define).
 * Contains the version string from package.json.
 * Undefined in dev mode (tsx) — callers should provide a fallback.
 */
declare const AGENTBNB_VERSION: string | undefined;
